---
phase: quick-260619-af
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/mobile/tamagui.config.ts
  - apps/mobile/constants/theme.ts
  - apps/mobile/app/game/results.tsx
  - apps/mobile/components/PackDetailsModal.tsx
  - apps/mobile/components/PlayerScoreCard.tsx
autonomous: true
requirements: []

must_haves:
  truths:
    - "App background is near-black (#0d0d0d), not dark blue (#1a1a2e)"
    - "Primary text is off-white (#f0f0f0), not pure white"
    - "Success/start action color is bold red (#e5191e), not forest green (#228b22)"
    - "Overlay uses rgba(255,255,255,0.15)"
    - "Six category colors are unchanged"
    - "Difficulty pill colors are unchanged"
  artifacts:
    - path: "apps/mobile/tamagui.config.ts"
      provides: "Updated dark theme with Option A palette"
      contains: "'#0d0d0d'"
    - path: "apps/mobile/constants/theme.ts"
      provides: "Updated SEMANTIC_COLORS with new accent and overlay"
      contains: "'#e5191e'"
  key_links:
    - from: "apps/mobile/app/game/results.tsx"
      to: "theme.background"
      via: "fallback hardcode replaced"
      pattern: "#0d0d0d"
    - from: "apps/mobile/components/PlayerScoreCard.tsx"
      to: "theme.background"
      via: "fallback hardcode replaced"
      pattern: "#0d0d0d"
    - from: "apps/mobile/components/PackDetailsModal.tsx"
      to: "SEMANTIC_COLORS.success"
      via: "inline hardcode replaced"
      pattern: "SEMANTIC_COLORS.success"
---

<objective>
Apply the "Option A — Newspaper / Editorial" color scheme to the mobile app.

Purpose: Replace the dark blue (#1a1a2e) theme with a near-black editorial palette that is more neutral and typographic. The bold red accent (#e5191e) replaces forest green as the primary action signal.
Output: Updated tamagui.config.ts, theme.ts, and three files with hardcoded color references replaced.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Tamagui config and SEMANTIC_COLORS with Option A palette</name>
  <files>apps/mobile/tamagui.config.ts, apps/mobile/constants/theme.ts</files>
  <action>
In apps/mobile/tamagui.config.ts, update the `dark` theme object with the Option A values:
- background: '#0d0d0d'   (was '#1a1a2e')
- color: '#f0f0f0'        (was '#ffffff')
- Add surface: '#1a1a1a'
- Add border: '#2e2e2e'
- Add accent: '#e5191e'
- Add muted: '#6b6b6b'
Leave all six category colors and the light theme unchanged.

In apps/mobile/constants/theme.ts, update SEMANTIC_COLORS:
- success: '#e5191e'              (was '#228b22' — bold red is now the positive/start action color)
- overlay: 'rgba(255,255,255,0.15)'  (was 'rgba(255,255,255,0.2)')
Leave error ('#dc143c') and remove ('#ff6b6b') unchanged.
  </action>
  <verify>
    <automated>grep -n "#0d0d0d\|#f0f0f0\|#e5191e\|rgba(255,255,255,0.15)" /Users/ffaber/claude-projects/trivial-world/apps/mobile/tamagui.config.ts /Users/ffaber/claude-projects/trivial-world/apps/mobile/constants/theme.ts</automated>
  </verify>
  <done>
tamagui.config.ts dark theme has background '#0d0d0d' and color '#f0f0f0'.
SEMANTIC_COLORS.success is '#e5191e' and overlay is 'rgba(255,255,255,0.15)'.
  </done>
</task>

<task type="auto">
  <name>Task 2: Replace hardcoded legacy colors in game, packs, and components</name>
  <files>
    apps/mobile/app/game/results.tsx,
    apps/mobile/components/PackDetailsModal.tsx,
    apps/mobile/components/PlayerScoreCard.tsx
  </files>
  <action>
Three files contain hardcoded legacy colors that must be replaced:

1. apps/mobile/app/game/results.tsx line 28:
   `theme.background?.val as string || '#1a1a2e'`
   → change fallback to `'#0d0d0d'`

2. apps/mobile/components/PlayerScoreCard.tsx line 34:
   `theme.background?.val as string || '#1a1a2e'`
   → change fallback to `'#0d0d0d'`

3. apps/mobile/components/PackDetailsModal.tsx lines 99 and 105:
   Both Pressable buttons use `backgroundColor: '#228b22'` (download and select buttons).
   These are start/positive-action buttons — replace both with `SEMANTIC_COLORS.success`.
   Add the import at the top of the file: `import { SEMANTIC_COLORS } from '../constants/theme';`
   (check whether the import already exists before adding it)

Do NOT change any category color references, difficulty pill colors (#4caf50, #ff9800, #f44336),
or error/remove colors (#dc143c, #ff6b6b).
  </action>
  <verify>
    <automated>grep -n "#1a1a2e\|#228b22" /Users/ffaber/claude-projects/trivial-world/apps/mobile/app/game/results.tsx /Users/ffaber/claude-projects/trivial-world/apps/mobile/components/PackDetailsModal.tsx /Users/ffaber/claude-projects/trivial-world/apps/mobile/components/PlayerScoreCard.tsx</automated>
  </verify>
  <done>
grep returns no matches — all three files have zero references to #1a1a2e or #228b22.
PackDetailsModal imports and uses SEMANTIC_COLORS.success for both action buttons.
  </done>
</task>

</tasks>

<verification>
Run full test suite to confirm no regressions:

```bash
cd /Users/ffaber/claude-projects/trivial-world && npx vitest run
```

All tests must pass (currently 400 passing).
</verification>

<success_criteria>
- app background is #0d0d0d (near-black editorial)
- primary text is #f0f0f0 (off-white)
- success/start action color is #e5191e (bold red) in both SEMANTIC_COLORS and PackDetailsModal buttons
- overlay is rgba(255,255,255,0.15)
- zero occurrences of #1a1a2e or #228b22 anywhere in game/, packs/, components/
- six category colors and difficulty pill colors unchanged
- 400 tests still passing
</success_criteria>

<output>
After completion, create `.planning/quick/260619-af-option-a-color-scheme/260619-af-SUMMARY.md`
</output>
