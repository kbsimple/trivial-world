---
phase: 24-remove-native-build-path
plan: 02
type: execute
wave: 2
depends_on: ["24-01"]
files_modified:
  - apps/mobile/app/index.tsx
  - apps/mobile/app/packs/index.tsx
  - apps/mobile/app/_layout.tsx
  - apps/mobile/app/game/_layout.tsx
  - apps/mobile/app/game/setup.tsx
  - apps/mobile/app/packs/combos.tsx
  - apps/mobile/stores/packStore.ts
  - apps/mobile/stores/questionStore.ts
  - apps/mobile/utils/haptics.ts
  - apps/mobile/components/PauseOverlay.tsx
  - apps/mobile/services/packIndex.ts
  - apps/mobile/services/questionProvider.ts
  - apps/mobile/services/packCache.ts
autonomous: true
requirements:
  - GOAL-collapse-Platform-OS-branches
  - GOAL-remove-dynamic-watermelondb-imports

must_haves:
  truths:
    - "No production source file in apps/mobile contains a Platform.OS conditional or a dynamic import of @nozbe/watermelondb"
    - "The web branch (formerly the Platform.OS === 'web' path) is inlined as the only path in every previously-branched file"
    - "services/packCache.ts web path is the sole pack-cache implementation"
    - "stores/packStore.ts no longer imports packDownloader (deleted in 24-01) — web IDB cache via packCache.ts is the sole download path"
    - "stores/questionStore.ts no longer dynamically imports @nozbe/watermelondb Q"
    - "services/questionProvider.ts no longer dynamically imports @nozbe/watermelondb Q"
    - "utils/haptics.ts and components/PauseOverlay.tsx use the web/no-op branch as the only path"
  artifacts:
    - path: "apps/mobile/stores/packStore.ts"
      provides: "web-only pack store using IDB cache (no packDownloader import, no Platform.OS)"
    - path: "apps/mobile/stores/questionStore.ts"
      provides: "web-only question store (no WatermelonDB Q import, no Platform.OS)"
    - path: "apps/mobile/services/questionProvider.ts"
      provides: "IDB-first question provider (no WatermelonDB Q import, no Platform.OS)"
    - path: "apps/mobile/services/packIndex.ts"
      provides: "offline-fallback pack index (no Platform.OS branch)"
    - path: "apps/mobile/services/packCache.ts"
      provides: "single web pack cache (no Platform.OS branch)"
  key_links:
    - from: "apps/mobile/stores/packStore.ts"
      to: "apps/mobile/services/packCache.ts"
      via: "import of packCache web API (replaces deleted packDownloader)"
      pattern: "from '\\.\\./services/packCache'"
    - from: "apps/mobile/stores/questionStore.ts"
      to: "apps/mobile/services/questionProvider.ts"
      via: "questionProvider is the web/IDB question source"
      pattern: "questionProvider"
---

<objective>
Collapse every `Platform.OS === 'web'` / `=== 'native'` branch in stores/services/UI to the web path as the only path. Inline the web branch, delete the native branch and the Platform.OS conditional. Remove the dynamic `await import('@nozbe/watermelondb')` blocks from stores and services (the database/ directory was deleted in 24-01, so these imports would fail at runtime).

Purpose: After 24-01 removed native code and config, the remaining Platform.OS branches are dead code on the native side. Collapsing to web-only removes the last surface area referencing the deleted native database.
Output: Production source files with a single web-only code path each; no Platform.OS conditionals, no dynamic WatermelonDB imports.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/24-remove-native-build-path/24-CONTEXT.md
@.planning/phases/24-remove-native-build-path/24-01-PLAN.md
@apps/mobile/services/platformStorage.ts
@apps/mobile/services/packCache.ts
@apps/mobile/services/packCache.web.ts
@apps/mobile/services/questionProvider.ts
@apps/mobile/services/packIndex.ts
@apps/mobile/stores/packStore.ts
@apps/mobile/stores/questionStore.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Collapse Platform.OS branches in services (packIndex, questionProvider, packCache)</name>
  <files>
    apps/mobile/services/packIndex.ts,
    apps/mobile/services/questionProvider.ts,
    apps/mobile/services/packCache.ts
  </files>
  <read_first>
    apps/mobile/services/packIndex.ts (identify the Platform.OS === 'web' branch and the native branch),
    apps/mobile/services/questionProvider.ts (identify the Platform.OS branches and the two `const { Q } = await import('@nozbe/watermelondb')` blocks at lines ~143 and ~246),
    apps/mobile/services/packCache.ts (identify the Platform.OS branch),
    apps/mobile/services/packCache.web.ts (web implementation already used by the web branch — keep as the canonical impl),
    apps/mobile/services/packCache.web.test.ts and apps/mobile/services/packIndex.web.test.ts (existing web-path tests — these already assert the web behavior and should continue to pass unchanged)
  </read_first>
  <action>
For each of `services/packIndex.ts`, `services/questionProvider.ts`, `services/packCache.ts`:

1. Remove every `if (Platform.OS === 'web') { ... } else { ... }` (or `=== 'native'`) conditional.
2. Inline the body of the web branch as the only code path; delete the native branch entirely.
3. Remove the `import { Platform } from 'react-native'` if it becomes unused after the collapse.
4. In `services/questionProvider.ts`: remove both `const { Q } = await import('@nozbe/watermelondb')` blocks (around lines 143 and 246). Replace the native-path logic that used `Q` with the web-path logic already present in the web branch (IDB-first via services/packCache.web.ts). If a function had a Platform.OS split where the web branch returned an IDB-backed result and the native branch did a WatermelonDB `Q.where(...)` query, the post-collapse function must only contain the IDB-backed result path.
5. In `services/packCache.ts`: collapse the Platform.OS branch — the web path (IDB via idb-keyval, delegating to packCache.web.ts) becomes the sole implementation. Delete any native branch that referenced the deleted database/ directory.
6. In `services/packIndex.ts`: collapse the offline-fallback Platform.OS branch — inline the web branch as the only path.

Do NOT modify `services/packCache.web.ts` (it is already the web impl and is referenced by the web branches being inlined).
Do NOT modify the existing web-path tests (`packCache.web.test.ts`, `packIndex.web.test.ts`, `questionProvider.web.test.ts`) — they should continue to pass unchanged.
  </action>
  <verify>
    <automated>cd apps/mobile && ! grep -rn "Platform.OS" services/packIndex.ts services/questionProvider.ts services/packCache.ts && ! grep -rn "import.*watermelondb" services/packIndex.ts services/questionProvider.ts services/packCache.ts && npx vitest run services/packCache.web.test.ts services/packIndex.web.test.ts services/questionProvider.web.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "Platform.OS" apps/mobile/services/packIndex.ts apps/mobile/services/questionProvider.ts apps/mobile/services/packCache.ts` returns no matches.
    - `grep -rn "watermelondb" apps/mobile/services/packIndex.ts apps/mobile/services/questionProvider.ts apps/mobile/services/packCache.ts` returns no matches.
    - `npx vitest run apps/mobile/services/packCache.web.test.ts apps/mobile/services/packIndex.web.test.ts apps/mobile/services/questionProvider.web.test.ts` exits 0.
  </acceptance_criteria>
  <done>Service-layer Platform.OS branches collapsed; no WatermelonDB imports remain in services; existing web-path tests still pass.</done>
</task>

<task type="auto">
  <name>Task 2: Collapse Platform.OS branches in stores and UI/utils/components</name>
  <files>
    apps/mobile/stores/packStore.ts,
    apps/mobile/stores/questionStore.ts,
    apps/mobile/utils/haptics.ts,
    apps/mobile/components/PauseOverlay.tsx,
    apps/mobile/app/index.tsx,
    apps/mobile/app/packs/index.tsx,
    apps/mobile/app/_layout.tsx,
    apps/mobile/app/game/_layout.tsx,
    apps/mobile/app/game/setup.tsx,
    apps/mobile/app/packs/combos.tsx
  </files>
  <read_first>
    apps/mobile/stores/packStore.ts (note line 7 `import { downloadPackWithProgress, getDownloadedPackIds, setActivePack } from '../services/packDownloader'` — packDownloader was deleted in 24-01; replace with the packCache web API; note line 4 platformStorage import which remains; note line 257 zustand persist using platformStorage which stays),
    apps/mobile/stores/questionStore.ts (note four `const { Q } = await import('@nozbe/watermelondb')` blocks at lines ~81, ~173, ~208, ~244; note platformStorage import stays; note line 286 zustand persist),
    apps/mobile/utils/haptics.ts (Platform.OS branch — web branch is the no-op),
    apps/mobile/components/PauseOverlay.tsx (Platform.OS branch),
    apps/mobile/app/index.tsx (note line 57 `const { Q } = await import('@nozbe/watermelondb')` block and the surrounding `if (Platform.OS === 'native')` branch that loads pack name from the deleted database; the web branch loads pack name from the availablePacks list already in the store — keep that web branch as the only path),
    apps/mobile/app/packs/index.tsx,
    apps/mobile/app/_layout.tsx,
    apps/mobile/app/game/_layout.tsx (note line 59 `const { Q } = await import('@nozbe/watermelondb')` block — same collapse pattern as app/index.tsx),
    apps/mobile/app/game/setup.tsx (note line 10 `import type { QuestionPackModel } from '../../database/models'`, line 58 `const { getDatabase } = await import('../../database')`, line 59 `const { Q } = await import('@nozbe/watermelondb')` — same native-fallback collapse pattern; the web branch loads the pack name from the in-memory `availablePacks.find` lookup at lines ~50-53),
    apps/mobile/app/packs/combos.tsx,
    apps/mobile/services/packCache.ts (after Task 1 collapse — to learn the web API surface that packStore must call instead of the deleted packDownloader)
  </read_first>
  <action>
**stores/packStore.ts:**
- Remove the `import { downloadPackWithProgress, getDownloadedPackIds, setActivePack } from '../services/packDownloader'` line.
- Replace each call to `downloadPackWithProgress`, `getDownloadedPackIds`, `setActivePack` with the equivalent web-path API from `services/packCache.ts` (after Task 1 collapse, packCache exposes the web IDB-backed functions: e.g., `getCachedPackIds`, `cachePack`, `setActivePackId` — read the post-Task-1 packCache.ts to get exact exported names and signatures; the executor MUST use the actual exported function names, not invent them).
- Add `import { <webApiFns> } from '../services/packCache'` with the exact names read from packCache.ts.
- Replace the dynamic `const { ... } = await import('../services/packCache.web')` at line ~129 with a static top-of-file `import { <webApiFns> } from '../services/packCache'` (the canonical shim — after Task 1 collapse, packCache.ts unconditionally re-exports the packCache.web API with the same names). Do not import `packCache.web` directly from stores.
- Remove any `if (Platform.OS === 'native')` branches that called packDownloader; keep only the web-path logic.
- Keep the `platformStorage` import and the zustand persist `storage: createJSONStorage(() => platformStorage)` at line ~257 — web platformStorage.ts remains the sole storage adapter.

**stores/questionStore.ts:**
- Remove all four `const { Q } = await import('@nozbe/watermelondb')` blocks (lines ~81, ~173, ~208, ~244).
- For each function containing a `Q` block, there was a Platform.OS split: the web branch already used the IDB-backed questionProvider; the native branch used WatermelonDB `Q.where(...)`. Inline the web branch as the only path; delete the native branch.
- Remove `import { Platform } from 'react-native'` if it becomes unused after collapse.
- Keep the `platformStorage` import and zustand persist config (line ~286) unchanged.

**utils/haptics.ts:** Collapse the Platform.OS branch — the web branch is the no-op (`return` or empty function). Inline the no-op as the only path. Remove `import { Platform } from 'react-native'`.

**components/PauseOverlay.tsx:** Same collapse pattern — inline the web branch as the only path, remove `import { Platform } from 'react-native'` if unused.

**app/index.tsx:** Remove the `const { Q } = await import('@nozbe/watermelondb')` block (line ~57) and its surrounding native branch. The web branch already loads the active pack name from `availablePacks` in the pack store — keep that as the only path. Remove the dynamic `import('../../database')` if present. Remove `import { Platform } from 'react-native'` if it becomes unused.

**app/game/_layout.tsx:** Same collapse as app/index.tsx (line ~59 `Q` import block). Inline the web branch as the only path.

**app/game/setup.tsx:** Collapse the `loadPackName` effect to the web path as the only path. Keep the in-memory `availablePacks.find(...)` lookup (lines ~50-53) that resolves the pack name from the store. Delete the native fallback (lines ~56-72): the `const { getDatabase } = await import('../../database')`, `const { Q } = await import('@nozbe/watermelondb')`, the `database.get<QuestionPackModel>('question_packs')` query, and the surrounding `if (Platform.OS === 'native')` branch. Remove the `import type { QuestionPackModel } from '../../database/models'` at line 10. Remove `import { Platform } from 'react-native'` if it becomes unused.

**app/packs/index.tsx, app/_layout.tsx, app/packs/combos.tsx:** For each, collapse the Platform.OS branch — inline the web branch as the only path, remove `import { Platform } from 'react-native'` if unused.

Do NOT modify the test files in this plan (`stores/questionStore.test.ts`, `stores/packStore.test.ts`) — those are rewritten in 24-03. Tests will be failing after this task because they still mock the native path; that is expected and will be addressed in 24-03.
  </action>
  <verify>
    <automated>cd apps/mobile && ! grep -rn "Platform.OS" app/ stores/packStore.ts stores/questionStore.ts utils/haptics.ts components/PauseOverlay.tsx && ! grep -rn "import.*watermelondb" app/ stores/packStore.ts stores/questionStore.ts utils/haptics.ts components/PauseOverlay.tsx && ! grep -rn "packDownloader" stores/packStore.ts && ! grep -rn "from.*'../database'\|from.*'../../database'" app/ stores/ && ! grep -rn "packCache.web" stores/ && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "Platform.OS" apps/mobile/app apps/mobile/stores/packStore.ts apps/mobile/stores/questionStore.ts apps/mobile/utils/haptics.ts apps/mobile/components/PauseOverlay.tsx` returns no matches.
    - `grep -rn "watermelondb" apps/mobile/app apps/mobile/stores/packStore.ts apps/mobile/stores/questionStore.ts apps/mobile/utils/haptics.ts apps/mobile/components/PauseOverlay.tsx` returns no matches.
    - `grep -rn "packDownloader" apps/mobile/stores/packStore.ts` returns no matches.
    - `grep -rn "from.*'../database'\|from.*'../../database'" apps/mobile/app apps/mobile/stores` returns no matches.
    - `grep -rn "packCache.web" apps/mobile/stores` returns no matches (stores import the canonical packCache.ts shim, not packCache.web.ts directly).
    - `apps/mobile/stores/packStore.ts` contains `from '../services/packCache'` (the new import replacing packDownloader).
    - Note: full test suite will not be green until 24-03 rewrites the native-path tests. That is expected — this plan only verifies structural collapse, not test greenness.
  </acceptance_criteria>
  <done>All Platform.OS branches collapsed to web-only in app/, stores/, utils/, components/; no WatermelonDB or packDownloader or database/ imports remain in production source; packStore re-wired to packCache web API.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| IDB cache → zustand stores | Pack cache (services/packCache.web.ts) is the sole source of downloaded pack data for stores |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-04 | Denial of Service | Collapsed web branch could throw if a previously-native-only code path is invoked without the IDB fallback | mitigate | Each collapsed function retains the existing web branch's try/catch and IDB-fallback behavior unchanged — only the dead native branch is deleted. Existing web-path tests (`packCache.web.test.ts`, `packIndex.web.test.ts`, `questionProvider.web.test.ts`) must continue to pass per Task 1 acceptance criteria. |
| T-24-05 | Tampering | packStore re-wired to packCache API — wrong function name could silently no-op | mitigate | Task 2 acceptance criteria requires `from '../services/packCache'` import and 24-03 runs the full test suite including the rewritten `packStore.test.ts` which asserts the web-path behavior. |
</threat_model>

<verification>
- All Platform.OS branches collapsed across the 13 listed files.
- No WatermelonDB dynamic imports remain in production source.
- No packDownloader or database/ imports remain in production source.
- packStore re-wired to packCache web API.
- Existing web-path service tests still pass (Task 1 acceptance).
- Note: stores/* tests will be failing until 24-03 — this is expected and not a regression of this plan.
</verification>

<success_criteria>
Production source contains zero Platform.OS conditionals, zero WatermelonDB imports, zero packDownloader references, zero database/ imports. Web path is the only path. Ready for 24-03 to rewrite native-path tests and run the phase gates.
</success_criteria>

<output>
After completion, create `.planning/phases/24-remove-native-build-path/24-02-SUMMARY.md`
</output>