---
milestone: v12.0
name: Web-Only Collapse
audited: 2026-07-19
status: passed
scores:
  requirements: 17/17
  phases: 1/1
  integration: 5/5
  flows: 5/5
gaps:
  requirements: []
  integration: []
  flows: []
tech_debt:
  - phase: 24-remove-native-build-path
    items:
      - "PRE-EXISTING (not introduced by v12.0): availablePacks is not auto-populated at boot — fetchAvailablePacks() is called only from app/packs/index.tsx:79. On a PWA reload with a persisted activePackId, the pack name renders blank on /game/setup and the turn-screen progress strip until the user navigates to /packs. The pre-24-02 web branch had identical behavior (the WatermelonDB lookup that resolved reload names was the native branch, now deleted). Cleanup: call fetchAvailablePacks() at boot in app/_layout.tsx or app/index.tsx (SW-cached pack index / IDB fallback serves it offline). Out of scope for the deletion milestone — candidate for a future UX phase."
      - "LOW (24-REVIEW.md): apps/mobile/app/packs/index.tsx:90-96 retains a dead setDownloadedPackVersions({}) effect + checkHasUpdateAvailable (always returns false post-collapse). Documented as the intended collapse pattern; wired into UI hasUpdate prop. Non-blocking cleanup opportunity."
      - "DEFERRED: Optional manual PWA smoke test (serve apps/mobile/dist/, verify SW registration + offline reload in a headed browser). The pnpm build:web gate producing dist/sw.js + 'SW: precached 7 files' log is the automated proxy, so this does not block passed status."
---

# v12.0 Web-Only Collapse — Milestone Audit

**Audited:** 2026-07-19
**Status:** ✅ passed
**Milestone goal:** Remove the native (Android/iOS) build path and the WatermelonDB native database layer, collapsing the app to a single web/PWA target.

## Scope

Single-phase deletion milestone: **Phase 24 — Remove Native Android/iOS Build Path**. No formal REQUIREMENTS.md REQ-IDs (none exists); the phase goal + three gates are the definition of done. Phase 24 depends on Phase 23 (Web Pack Downloading) — the IDB + Service Worker layer is the sole persistence layer after native removal.

## Phase Verification

| Phase | Verification Status | Score | Report |
|-------|---------------------|-------|--------|
| 24 | passed | 17/17 must-haves | 24-VERIFICATION.md |

All 17 must-haves verified against the live codebase (native artifacts gone, deps removed, config stripped, branches collapsed, README web/PWA-only). All three gates re-confirmed green by the verifier:
- Gate 1 (vitest): 433/433 passed, exit 0
- Gate 2 (tsc --noEmit): exit 0, 0 errors
- Gate 3 (build:web): exit 0, dist/ produced with sw.js + precache

## Requirements Coverage

No REQUIREMENTS.md traceability table — the phase goal's enumerated items are the requirements. 3-source cross-reference (VERIFICATION + SUMMARY + ROADMAP) confirms every enumerated deletion/rewrite + all 3 gates are covered by plans 24-01/24-02/24-03 and verified passed. No orphaned requirements.

## Cross-Phase Integration

Integration checker verdict: **INTEGRATION PASSED** — 5/5 E2E flows wired, 0 broken, 0 orphaned exports, 0 missing connections.

| Flow | Status | Evidence |
|------|--------|----------|
| 1. Pack download (packs UI → packStore → packCache → IDB) | PASS | packStore.downloadPack delegates to downloadPackForOffline; 59 packStore tests pass |
| 2. Question flow (questionStore → questionProvider → IDB-first) | PASS | questionStore.selectQuestion → getNextQuestion; Q blocks removed; 41 tests pass |
| 3. Pack name resolution (availablePacks.find) | PASS | app/index.tsx:54, app/game/setup.tsx:47, app/game/turn.tsx:124 (pre-existing boot-population gap noted in tech debt) |
| 4. Offline reload (SW + IDB) | PASS | dist/sw.js present; packIndex IDB write-through + fallback unchanged from Phase 23 |
| 5. Persistence (platformStorage / sessionStorage) | PASS | all 4 stores retain platformStorage import + zustand persist |

## Code Review

24-REVIEW.md: 2 LOW findings, no blockers/high/medium.
- LOW (fixed): stale platformStorage.ts comment referencing deleted native/AsyncStorage path — fixed in commit 7b91e7d.
- LOW (left): dead checkHasUpdateAvailable / setDownloadedPackVersions in packs/index.tsx — documented intended collapse pattern, wired into UI; left as non-blocking cleanup.

## Nyquist Compliance

Skipped — `workflow.nyquist_validation: false` in .planning/config.json.

## Tech Debt

See frontmatter `tech_debt`. Three items, all non-blocking:
1. **Pre-existing** availablePacks boot-time population gap (not a v12.0 regression) — candidate for a future UX phase.
2. **LOW** dead update-available code in packs/index.tsx — intentional, cleanup opportunity.
3. **Deferred** optional manual PWA smoke test — automated proxy (build:web sw.js precache) satisfies the gate.

## Verdict

Milestone v12.0 achieved its definition of done. The app is fully web/PWA-only: zero Platform.OS conditionals, zero WatermelonDB imports, zero database/ imports in production source; IDB (idb-keyval) + sessionStorage are the sole persistence layers; Service Worker offline layer intact. All gates green, integration passed, no critical gaps. Tech debt is minimal and pre-existing. **Passed** — proceed to complete milestone.