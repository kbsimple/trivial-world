---
phase: "10"
plan: "02"
type: execute
wave: 2
depends_on: ["10-01"]
files_modified: []
autonomous: false
requirements:
  - GEN-01
  - NETL-01
  - NETL-02
  - NETL-03
  - PWA-02

must_haves:
  truths:
    - "Two Netlify sites exist (trivial-world-game and trivial-world-generator)"
    - "Both sites are connected to the GitHub repository"
    - "Both sites deploy automatically on push to main branch"
    - "Both sites are accessible via HTTPS"
    - "SPA redirects work (deep linking returns 200 status)"
  artifacts:
    - path: "Netlify Site: trivial-world-game"
      provides: "Game app deployment"
      contains: "Publish: apps/mobile/dist"
      note: "External infrastructure resource managed via Netlify UI - not a file artifact"
    - path: "Netlify Site: trivial-world-generator"
      provides: "Generator app deployment"
      contains: "Publish: apps/generator/out"
      note: "External infrastructure resource managed via Netlify UI - not a file artifact"
  key_links:
    - from: "GitHub main branch"
      to: "Netlify Build"
      via: "GitHub integration"
      pattern: "push to main triggers build"
    - from: "Netlify Build"
      to: "Netlify CDN"
      via: "deployment pipeline"
      pattern: "successful build deploys to CDN"
---

<objective>
Configure two Netlify sites for the monorepo and verify automatic deployment from GitHub.

Purpose: Enable continuous deployment where pushes to main branch automatically deploy both apps. Verify that SPA redirects (configured in Plan 10-01) work correctly for deep linking.
Output: Two live Netlify sites accessible via HTTPS with working SPA redirects.

Note: This plan requires manual steps in Netlify UI. The implementation tasks document the exact configuration needed. Artifacts are external infrastructure (Netlify sites) managed via UI, not file artifacts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-netlify-deployment/10-CONTEXT.md
@.planning/phases/10-netlify-deployment/10-RESEARCH.md

<interfaces>
<!-- Build outputs from Phase 9 (already verified in 10-01) -->
From turbo.json:
```json
{
  "tasks": {
    "build": {
      "outputs": ["dist/**", ".expo/**", "out/**"]
    }
  }
}
```

<!-- Publish directories -->
- Game app: apps/mobile/dist/ (Expo static export)
- Generator app: apps/generator/out/ (Next.js static export)
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Create Netlify site for game app</name>
  <what_built>Configuration files from Plan 10-01 are committed. Build succeeds locally with pnpm build.</what_built>
  <how_to_verify>
    **Manual steps in Netlify UI (https://app.netlify.com):**

    1. Click "Add new site" → "Import an existing project"
    2. Connect to GitHub (authorize if first time)
    3. Select repository: `trivial-world`
    4. Configure build settings:
       - **Site name:** `trivial-world-game`
       - **Branch:** `main`
       - **Build command:** `pnpm install && pnpm build`
       - **Publish directory:** `apps/mobile/dist`
    5. Click "Deploy site"
    6. Wait for first deploy to complete

    **Expected result:** Site URL like `https://trivial-world-game.netlify.app` shows the game app.
  </how_to_verify>
  <resume_signal>Type "game site created" or describe issues encountered</resume_signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Create Netlify site for generator app</name>
  <what_built>Game app Netlify site is live.</what_built>
  <how_to_verify>
    **Manual steps in Netlify UI (https://app.netlify.com):**

    1. Click "Add new site" → "Import an existing project"
    2. Select same repository: `trivial-world`
    3. Configure build settings:
       - **Site name:** `trivial-world-generator`
       - **Branch:** `main`
       - **Build command:** `pnpm install && pnpm build`
       - **Publish directory:** `apps/generator/out`
    4. Click "Deploy site"
    5. Wait for first deploy to complete

    **Expected result:** Site URL like `https://trivial-world-generator.netlify.app` shows the generator app.
  </how_to_verify>
  <resume_signal>Type "generator site created" or describe issues encountered</resume_signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify HTTPS and deep linking on game app</name>
  <what_built>Both Netlify sites are live.</what_built>
  <how_to_verify>
    **Verification steps:**

    1. Open game app URL: `https://trivial-world-game.netlify.app`
    2. Verify HTTPS is active (padlock icon in browser)
    3. Test SPA redirect by navigating to a non-root route:
       - Visit `https://trivial-world-game.netlify.app/game` directly (not via navigation)
       - The page should load (200 status) and show the app, NOT a 404
    4. Test another route:
       - Visit `https://trivial-world-game.netlify.app/setup`
       - Page should load correctly

    **Expected result:** All routes return 200 status, app loads correctly, HTTPS is enforced.
  </how_to_verify>
  <resume_signal>Type "game app verified" or describe issues encountered</resume_signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Verify HTTPS and deep linking on generator app</name>
  <what_built>Game app verification complete.</what_built>
  <how_to_verify>
    **Verification steps:**

    1. Open generator app URL: `https://trivial-world-generator.netlify.app`
    2. Verify HTTPS is active (padlock icon in browser)
    3. Test SPA redirect by navigating to a non-root route:
       - Visit `https://trivial-world-generator.netlify.app/generate` directly
       - The page should load (200 status) and show the app, NOT a 404
    4. Test another route:
       - Visit `https://trivial-world-generator.netlify.app/review`
       - Page should load correctly

    **Expected result:** All routes return 200 status, app loads correctly, HTTPS is enforced.
  </how_to_verify>
  <resume_signal>Type "generator app verified" or describe issues encountered</resume_signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: Verify automatic deployment on push</name>
  <what_built>Both sites are live and verified.</what_built>
  <how_to_verify>
    **Verification steps:**

    1. Make a small change to the game app (e.g., update version in package.json)
    2. Commit and push to main branch:
       ```bash
       git add apps/mobile/package.json
       git commit -m "test: verify auto-deploy"
       git push origin main
       ```
    3. Go to Netlify UI → trivial-world-game → Deploys
    4. Verify a new deploy was triggered automatically
    5. Wait for deploy to complete
    6. Verify the change is live on the site

    **Expected result:** Push to main branch triggers automatic build and deploy on both sites.
  </how_to_verify>
  <resume_signal>Type "auto-deploy verified" or describe issues encountered</resume_signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Repository → Netlify Build | Code pushed to main branch triggers untrusted build |
| Netlify CDN → Public Internet | Static sites accessible to anyone |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-06 | Spoofing | Netlify site | accept | Netlify manages *.netlify.app domain, no custom domain spoofing possible |
| T-10-07 | Tampering | Deployment | mitigate | Only main branch deploys to production, PR previews are isolated |
| T-10-08 | Information Disclosure | Build logs | accept | Build logs are private to Netlify account holder |

Threat model already addressed in Plan 10-01. This plan is verification-only.
</threat_model>

<verification>
## Manual Verification Checklist

1. **Game App Site:**
   - [ ] Site created at trivial-world-game.netlify.app
   - [ ] Build command: `pnpm install && pnpm build`
   - [ ] Publish directory: `apps/mobile/dist`
   - [ ] Branch: `main`
   - [ ] HTTPS active (Netlify default)

2. **Generator App Site:**
   - [ ] Site created at trivial-world-generator.netlify.app
   - [ ] Build command: `pnpm install && pnpm build`
   - [ ] Publish directory: `apps/generator/out`
   - [ ] Branch: `main`
   - [ ] HTTPS active (Netlify default)

3. **SPA Redirects (NETL-02):**
   - [ ] Game: `https://trivial-world-game.netlify.app/game` returns 200
   - [ ] Generator: `https://trivial-world-generator.netlify.app/generate` returns 200

4. **Auto-Deploy:**
   - [ ] Push to main triggers deploy on both sites
</verification>

<success_criteria>
- [ ] Two Netlify sites created (game and generator)
- [ ] Both sites connected to GitHub repository
- [ ] Build command configured correctly
- [ ] Publish directories set correctly (apps/mobile/dist, apps/generator/out)
- [ ] HTTPS enforced on both sites (Netlify default)
- [ ] SPA redirects work (direct URL access returns 200) - NETL-02 verified
- [ ] Auto-deploy works (push to main triggers build)
</success_criteria>

<output>
After completion, create `.planning/phases/10-netlify-deployment/10-02-SUMMARY.md`
</output>