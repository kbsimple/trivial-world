---
phase: 24-remove-native-build-path
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/mobile/app.config.js
  - apps/mobile/package.json
  - apps/mobile/metro.config.js
  - apps/mobile/babel.config.js
  - apps/mobile/__mocks__/react-native.ts
  - apps/mobile/database/index.ts
  - apps/mobile/database/schema.ts
  - apps/mobile/database/migrations/index.ts
  - apps/mobile/database/migrations/002_add_question_packs.ts
  - apps/mobile/database/migrations/003_add_tidbits.ts
  - apps/mobile/database/models/Question.ts
  - apps/mobile/database/models/QuestionPack.ts
  - apps/mobile/services/packDownloader.ts
  - apps/mobile/services/platformStorage.native.ts
  - apps/mobile/__mocks__/watermelondb.ts
  - apps/mobile/__mocks__/async-storage.ts
  - apps/mobile/assets/android-icon-background.png
  - apps/mobile/assets/android-icon-foreground.png
  - apps/mobile/assets/android-icon-monochrome.png
  - apps/mobile/dist-ios/
autonomous: true
requirements:
  - GOAL-delete-database-dir
  - GOAL-delete-packDownloader
  - GOAL-delete-platformStorage-native
  - GOAL-remove-watermelondb-dep
  - GOAL-remove-asyncstorage-dep
  - GOAL-remove-appconfig-android-ios-blocks
  - GOAL-remove-android-ios-scripts
  - GOAL-delete-android-icon-assets
  - GOAL-delete-dist-ios
  - GOAL-simplify-metro
  - GOAL-simplify-babel
  - GOAL-mocks-Platform-OS-web

must_haves:
  truths:
    - "The apps/mobile/database/ directory no longer exists"
    - "services/packDownloader.ts and services/platformStorage.native.ts no longer exist"
    - "package.json no longer lists @nozbe/watermelondb or @react-native-async-storage/async-storage"
    - "app.config.js has no ios or android blocks (web block retained)"
    - "package.json has no android or ios scripts; start is web-only"
    - "metro.config.js has no native-mock resolver block for watermelondb/async-storage"
    - "babel.config.js has no @babel/plugin-proposal-decorators or @babel/plugin-transform-class-properties"
    - "__mocks__/react-native.ts sets Platform.OS = 'web'"
    - "android icon assets (assets/android-icon-*.png) and empty dist-ios/ are deleted"
  artifacts:
    - path: "apps/mobile/app.config.js"
      provides: "web-only Expo config (no ios/android blocks)"
    - path: "apps/mobile/package.json"
      provides: "deps and scripts with native artifacts removed"
    - path: "apps/mobile/metro.config.js"
      provides: "simplified resolver without native-module mocking"
    - path: "apps/mobile/babel.config.js"
      provides: "babel config without WatermelonDB decorator plugins"
    - path: "apps/mobile/__mocks__/react-native.ts"
      provides: "RN mock with Platform.OS = 'web'"
  key_links:
    - from: "apps/mobile/services/platformStorage.ts"
      to: "stores/*"
      via: "platformStorage export (web sessionStorage impl) remains the sole storage adapter"
      pattern: "platformStorage"
---

<objective>
Delete every native-only artifact (database/ directory, packDownloader.ts, platformStorage.native.ts, android icon assets, empty dist-ios/) and strip native config (ios/android app.config blocks, android/ios npm scripts, @nozbe/watermelondb + @react-native-async-storage/async-storage deps, WatermelonDB decorator babel plugins, native-mock resolver in metro.config.js). Flip the RN mock to Platform.OS = 'web' so downstream tests exercise the web path.

Purpose: This plan clears the dead native code and config so plan 24-02 can collapse the remaining Platform.OS branches without leaving dangling imports.
Output: A web/PWA-only config surface; the codebase will not import WatermelonDB or AsyncStorage anywhere outside of test files (handled in 24-03).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-remove-native-build-path/24-CONTEXT.md
@apps/mobile/app.config.js
@apps/mobile/package.json
@apps/mobile/metro.config.js
@apps/mobile/babel.config.js
@apps/mobile/__mocks__/react-native.ts
@apps/mobile/services/platformStorage.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete native-only files, directories, and assets</name>
  <files>
    apps/mobile/database/index.ts,
    apps/mobile/database/schema.ts,
    apps/mobile/database/migrations/index.ts,
    apps/mobile/database/migrations/002_add_question_packs.ts,
    apps/mobile/database/migrations/003_add_tidbits.ts,
    apps/mobile/database/models/Question.ts,
    apps/mobile/database/models/QuestionPack.ts,
    apps/mobile/services/packDownloader.ts,
    apps/mobile/services/platformStorage.native.ts,
    apps/mobile/__mocks__/watermelondb.ts,
    apps/mobile/__mocks__/async-storage.ts,
    apps/mobile/assets/android-icon-background.png,
    apps/mobile/assets/android-icon-foreground.png,
    apps/mobile/assets/android-icon-monochrome.png,
    apps/mobile/dist-ios/
  </files>
  <read_first>
    apps/mobile/database/index.ts (confirm contents are WatermelonDB-only and not imported by web-path code),
    apps/mobile/services/packDownloader.ts (confirm only imported by stores/packStore.ts which will be rewritten in 24-02 — leaving a dangling import here is acceptable for this plan; 24-02 collapses the branches that import it),
    apps/mobile/services/platformStorage.native.ts (confirm it only re-exports AsyncStorage),
    apps/mobile/__mocks__/watermelondb.ts,
    apps/mobile/__mocks__/async-storage.ts
  </read_first>
  <action>
Delete the entire `apps/mobile/database/` directory (index.ts, schema.ts, migrations/, models/ — all WatermelonDB-only).
Delete `apps/mobile/services/packDownloader.ts` (native WatermelonDB download path; replaced by web IDB cache in services/packCache.ts which already exists).
Delete `apps/mobile/services/platformStorage.native.ts` (AsyncStorage re-export; web platformStorage.ts remains the sole implementation).
Delete the web-only mocks that existed to stub the native modules on web: `apps/mobile/__mocks__/watermelondb.ts` and `apps/mobile/__mocks__/async-storage.ts` (no longer needed once the deps are removed in Task 2).
Delete android icon assets: `apps/mobile/assets/android-icon-background.png`, `apps/mobile/assets/android-icon-foreground.png`, `apps/mobile/assets/android-icon-monochrome.png`.
Delete the empty `apps/mobile/dist-ios/` directory.

Do NOT delete `apps/mobile/services/platformStorage.ts` (the web sessionStorage implementation) — it remains the sole storage adapter for zustand persist.

Use `git rm -r` for tracked files/directories; use plain `rm -rf` for untracked ones (verify with `git status` after). Do not commit yet — Task 2 and 24-02 will further modify the repo.
  </action>
  <verify>
    <automated>test ! -e apps/mobile/database && test ! -e apps/mobile/services/packDownloader.ts && test ! -e apps/mobile/services/platformStorage.native.ts && test ! -e apps/mobile/__mocks__/watermelondb.ts && test ! -e apps/mobile/__mocks__/async-storage.ts && test ! -e apps/mobile/dist-ios && ! ls apps/mobile/assets/android-icon-*.png 2>/dev/null && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `test ! -e apps/mobile/database` passes (directory gone).
    - `test ! -e apps/mobile/services/packDownloader.ts` passes.
    - `test ! -e apps/mobile/services/platformStorage.native.ts` passes.
    - `test ! -e apps/mobile/__mocks__/watermelondb.ts` passes.
    - `test ! -e apps/mobile/__mocks__/async-storage.ts` passes.
    - `test ! -e apps/mobile/dist-ios` passes.
    - `ls apps/mobile/assets/android-icon-*.png 2>/dev/null` returns no matches.
    - `apps/mobile/services/platformStorage.ts` still exists (web impl retained).
  </acceptance_criteria>
  <done>All native-only files/directories/assets deleted; web platformStorage.ts retained.</done>
</task>

<task type="auto">
  <name>Task 2: Strip native config from app.config.js, package.json, metro.config.js, babel.config.js; flip RN mock to web</name>
  <files>
    apps/mobile/app.config.js,
    apps/mobile/package.json,
    apps/mobile/metro.config.js,
    apps/mobile/babel.config.js,
    apps/mobile/__mocks__/react-native.ts
  </files>
  <read_first>
    apps/mobile/app.config.js (full contents — note ios/android blocks and web block to retain),
    apps/mobile/package.json (full contents — note scripts and deps/devDeps),
    apps/mobile/metro.config.js (full contents — note resolver block to remove),
    apps/mobile/babel.config.js (full contents — confirm decorators plugins are only used by WatermelonDB models, which were deleted in Task 1; grep `@model\|@field\|@children\|@action\|@relation\|@lazy` across the codebase to confirm no other usage),
    apps/mobile/__mocks__/react-native.ts
  </read_first>
  <action>
**apps/mobile/app.config.js:** Remove the entire `ios: { ... }` and `android: { ... }` blocks. Keep the `web: { ... }` block, `name`, `slug`, `version`, `orientation`, `icon`, `userInterfaceStyle`, `scheme`, and the `plugins` array (leave plugins array as-is — its entries are already commented out). Update the file's leading comment: replace "Expo configuration for Trivial World mobile app" with "Expo configuration for Trivial World web/PWA app (web-only — native iOS/Android build path removed in Phase 24)".

**apps/mobile/package.json:** In `scripts`, remove the `android` and `ios` entries. Change `start` from `"expo start"` to `"expo start --web"` so the default dev server is web-only. Keep `web`, `build`, `build:web`, `test`, `lint`, `typecheck`. In `dependencies`, remove `"@nozbe/watermelondb": "^0.28.0"` and `"@react-native-async-storage/async-storage": "^2.1.0"`. In `devDependencies`, remove `"@babel/plugin-proposal-decorators": "^7.29.7"` and `"@babel/plugin-transform-class-properties": "^7.29.7"` (verified only used by deleted WatermelonDB models — grep `@model\|@field\|@children\|@action\|@relation\|@lazy` returns no remaining usage outside deleted `database/` files). Keep all other deps intact (expo-haptics has a web stub, react-native-reanimated/plugin stays in babel).

**apps/mobile/metro.config.js:** Remove the entire `config.resolver.resolveRequest = (context, moduleName, platform) => { ... }` block including the WatermelonDB, AsyncStorage, and expo-screen-orientation mock branches. The file should reduce to: `const { getDefaultConfig } = require('expo/metro-config'); const config = getDefaultConfig(__dirname); module.exports = config;` with an updated header comment explaining that platform-specific module mocking is no longer needed because native-only deps have been removed.

**apps/mobile/babel.config.js:** Remove the two decorator plugin entries: `['@babel/plugin-proposal-decorators', { legacy: true }]` and `['@babel/plugin-transform-class-properties', { loose: true }]`. Keep `babel-preset-expo`, the `@tamagui/babel-plugin` (with `disableExtraction: isWeb`), and `'react-native-reanimated/plugin'` (must remain last). Update the comment so it no longer mentions WatermelonDB decorators.

**apps/mobile/__mocks__/react-native.ts:** Change `export const Platform = { OS: 'ios' as const };` to `export const Platform = { OS: 'web' as const };`. Update the comment to state that Platform.OS = 'web' so tests exercise the web (IDB + sessionStorage) path; remove the "Platform.OS = 'ios' so tests exercise the native (WatermelonDB) code paths" sentence.

Do NOT run `pnpm install` yet — Task 24-03 will run the full gate (tests + tsc + build:web). But do run `git status` to confirm the changes are staged.
  </action>
  <verify>
    <automated>cd apps/mobile && ! grep -q "ios:" app.config.js && ! grep -q "android:" app.config.js && grep -q "web:" app.config.js && ! grep -q '"android"' package.json && ! grep -q '"ios"' package.json && grep -q '"start": "expo start --web"' package.json && ! grep -q "@nozbe/watermelondb" package.json && ! grep -q "@react-native-async-storage/async-storage" package.json && ! grep -q "plugin-proposal-decorators" package.json && ! grep -q "plugin-transform-class-properties" package.json && ! grep -q "resolveRequest" metro.config.js && ! grep -q "plugin-proposal-decorators" babel.config.js && ! grep -q "plugin-transform-class-properties" babel.config.js && grep -q "OS: 'web'" __mocks__/react-native.ts && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "ios:\|android:" apps/mobile/app.config.js` returns 0; `grep -c "web:" apps/mobile/app.config.js` returns >= 1.
    - `grep -c '"android"\|"ios"' apps/mobile/package.json` returns 0 (scripts removed); `grep -c '"start": "expo start --web"' apps/mobile/package.json` returns 1.
    - `grep -c "@nozbe/watermelondb\|@react-native-async-storage/async-storage" apps/mobile/package.json` returns 0.
    - `grep -c "plugin-proposal-decorators\|plugin-transform-class-properties" apps/mobile/package.json` returns 0.
    - `grep -c "plugin-proposal-decorators\|plugin-transform-class-properties" apps/mobile/babel.config.js` returns 0.
    - `grep -c "resolveRequest" apps/mobile/metro.config.js` returns 0.
    - `grep -c "OS: 'web'" apps/mobile/__mocks__/react-native.ts` returns 1.
    - Codebase-wide grep for remaining `@nozbe/watermelondb` or `@react-native-async-storage/async-storage` imports in `apps/mobile` (excluding `node_modules`, `dist/`) returns no production source matches (test files in stores/ will be handled in 24-03).
  </acceptance_criteria>
  <done>Native config stripped from app.config.js, package.json, metro.config.js, babel.config.js; RN mock flipped to web; web platformStorage.ts retained.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| localStorage/sessionStorage → zustand persist | Web persistence layer remains the single trust boundary for game/pack state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-01 | Information Disclosure | Deleted WatermelonDB SQLite store on user devices | accept | Native build path was never shipped to end users (web/PWA has been the live target); no user devices hold a WatermelonDB SQLite file to disclose. |
| T-24-02 | Data Loss | Offline cached packs in IDB after native removal | accept (no action) | IDB cache (services/packCache.web.ts) is unchanged in this plan — it remains the sole persistence layer for pack downloads. No mitigation required. |
| T-24-03 | Tampering | Stale dependency references could re-introduce native modules at install time | mitigate | Verified by acceptance criteria: package.json grep returns 0 for @nozbe/watermelondb and @react-native-async-storage/async-storage. `pnpm install` (run in 24-03) will prune lockfile. |
</threat_model>

<verification>
- All native-only files/directories/assets deleted (Task 1 acceptance criteria).
- app.config.js, package.json, metro.config.js, babel.config.js stripped of native config (Task 2 acceptance criteria).
- __mocks__/react-native.ts sets Platform.OS = 'web'.
- `git status` shows the expected deletions and modifications.
- Note: full test suite + tsc + build:web gates run in plan 24-03 after Platform.OS branches are collapsed.
</verification>

<success_criteria>
Native-only artifacts gone, native config gone from all four config files, RN mock flipped to web. Codebase is ready for 24-02 to collapse the remaining Platform.OS branches.
</success_criteria>

<output>
After completion, create `.planning/phases/24-remove-native-build-path/24-01-SUMMARY.md`
</output>