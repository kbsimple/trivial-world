---
phase: 24-remove-native-build-path
plan: 03
type: execute
wave: 3
depends_on: ["24-02"]
files_modified:
  - apps/mobile/stores/questionStore.test.ts
  - apps/mobile/stores/packStore.test.ts
  - README.md
autonomous: true
requirements:
  - GOAL-rewrite-questionStore-test-web-path
  - GOAL-rewrite-packStore-test-web-path
  - GOAL-update-readme-web-pwa-only
  - GOAL-gate-vitest-green
  - GOAL-gate-tsc-clean
  - GOAL-gate-build-web-succeeds

must_haves:
  truths:
    - "stores/questionStore.test.ts exercises the web path (IDB-backed questionProvider + zustand persist on platformStorage web impl) and passes"
    - "stores/packStore.test.ts exercises the web path (packCache web API + zustand persist) and passes"
    - "No test file mocks @nozbe/watermelondb or @react-native-async-storage/async-storage"
    - "README.md reflects web/PWA-only deployment (no iOS/Android instructions, no WatermelonDB reference)"
    - "npx vitest run exits 0 (all tests pass)"
    - "npx tsc --noEmit exits 0 with no NEW errors vs. pre-phase baseline"
    - "pnpm build:web succeeds (exits 0)"
  artifacts:
    - path: "apps/mobile/stores/questionStore.test.ts"
      provides: "web-path test coverage for questionStore"
    - path: "apps/mobile/stores/packStore.test.ts"
      provides: "web-path test coverage for packStore"
    - path: "README.md"
      provides: "web/PWA-only deployment documentation"
  key_links:
    - from: "apps/mobile/stores/packStore.test.ts"
      to: "apps/mobile/services/packCache.ts"
      via: "mocks/asserts against the packCache web API (replaces the deleted packDownloader mock)"
      pattern: "packCache"
    - from: "apps/mobile/stores/questionStore.test.ts"
      to: "apps/mobile/services/questionProvider.ts"
      via: "asserts web-path question provider behavior (replaces WatermelonDB Q mock)"
      pattern: "questionProvider"
---

<objective>
Rewrite the two native-path tests (`stores/questionStore.test.ts`, `stores/packStore.test.ts`) to cover the web path now that Platform.OS has been collapsed to web-only in 24-02. Remove any remaining `vi.mock('@nozbe/watermelondb', ...)` / `vi.mock('@react-native-async-storage/async-storage', ...)` lines. Update README.md to reflect web/PWA-only deployment. Run the three phase gates: full vitest suite green, `tsc --noEmit` clean, `pnpm build:web` succeeds.

Purpose: The stores tests were the only remaining references to the native path. After rewrite, the codebase is fully web/PWA-only and the phase gates prove it.
Output: Web-path test coverage for both stores; updated README; green gates.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/24-remove-native-build-path/24-CONTEXT.md
@.planning/phases/24-remove-native-build-path/24-01-PLAN.md
@.planning/phases/24-remove-native-build-path/24-02-PLAN.md
@CLAUDE.md
@apps/mobile/stores/packStore.ts
@apps/mobile/stores/questionStore.ts
@apps/mobile/stores/packStore.test.ts
@apps/mobile/stores/questionStore.test.ts
@apps/mobile/services/packCache.ts
@apps/mobile/services/questionProvider.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rewrite stores/packStore.test.ts and stores/questionStore.test.ts for the web path</name>
  <files>
    apps/mobile/stores/packStore.test.ts,
    apps/mobile/stores/questionStore.test.ts
  </files>
  <read_first>
    apps/mobile/stores/packStore.test.ts (full current contents — note the `vi.mock('../services/packDownloader', ...)` at line ~35-36, the `vi.mock('@react-native-async-storage/async-storage', ...)` at line ~16, and the imports at line ~63; these mocks must be removed/replaced),
    apps/mobile/stores/questionStore.test.ts (full current contents — note the `vi.mock('@react-native-async-storage/async-storage', ...)` at line ~61 and any `vi.mock('@nozbe/watermelondb', ...)` or `vi.mock('../database', ...)` blocks),
    apps/mobile/stores/packStore.ts (the post-24-02 version — confirm packStore now imports from '../services/packCache' and what functions it calls; the test must mock/assert against that API),
    apps/mobile/stores/questionStore.ts (the post-24-02 version — confirm the web-path functions called and what the test should assert),
    apps/mobile/services/packCache.ts (post-24-02 — learn the exact exported function names/signatures the test should mock),
    apps/mobile/services/questionProvider.ts (post-24-02 — learn the exact exported function names/signatures),
    apps/mobile/services/packCache.web.test.ts and apps/mobile/services/questionProvider.web.test.ts (existing web-path test patterns to mirror in the store tests — match their mock setup for IDB/sessionStorage)
  </read_first>
  <behavior>
    - packStore.test.ts: adding/activating a pack via the web IDB cache path results in `availablePacks` containing the pack and `activePackId` being set to the pack id.
    - packStore.test.ts: removing a pack via the web path removes it from `availablePacks`.
    - packStore.test.ts: persisted state is restored from platformStorage (web sessionStorage mock) on store rehydration.
    - questionStore.test.ts: marking a question as asked via the web path updates `askedQuestionIds` to include the question id.
    - questionStore.test.ts: resetting asked questions clears `askedQuestionIds`.
    - questionStore.test.ts: persisted state is restored from platformStorage (web sessionStorage mock) on store rehydration.
    - Both files: no `vi.mock('@nozbe/watermelondb', ...)`, no `vi.mock('@react-native-async-storage/async-storage', ...)`, no `vi.mock('../services/packDownloader', ...)`, no `vi.mock('../database', ...)`.
  </behavior>
  <action>
**stores/packStore.test.ts:**
1. Delete `vi.mock('../services/packDownloader', ...)` and `vi.mock('@react-native-async-storage/async-storage', ...)`.
2. Remove imports from `'../services/packDownloader'`.
3. Add `vi.mock('../services/packCache', ...)` mocking the exact functions packStore now imports (read the post-24-02 packStore.ts to get the names; common surface: `getCachedPackIds`, `cachePack`, `setActivePackId`, `removeCachedPack` — use the ACTUAL exported names from packCache.ts).
4. Provide a sessionStorage mock for platformStorage (match the pattern in packCache.web.test.ts) so zustand persist has a storage adapter.
5. Rewrite test cases to assert the web-path behaviors listed in <behavior>: adding/activating a pack via packCache, removing via packCache, and rehydration from sessionStorage. Use the actual store actions exposed by packStore.ts (e.g., `addPack`, `setActivePack`, `removePack`, `loadCachedPacks` — use ACTUAL action names from the store).
6. Ensure every assertion runs against the post-24-02 web-only store API.

**stores/questionStore.test.ts:**
1. Delete `vi.mock('@react-native-async-storage/async-storage', ...)` and any `vi.mock('@nozbe/watermelondb', ...)` or `vi.mock('../database', ...)` blocks.
2. Add `vi.mock('../services/questionProvider', ...)` if the test asserts provider behavior; otherwise test the store directly with a sessionStorage mock.
3. Provide a sessionStorage mock for platformStorage (match packCache.web.test.ts pattern).
4. Rewrite test cases to assert: marking a question asked updates `askedQuestionIds`, resetting clears it, persisted state rehydrates from sessionStorage. Use ACTUAL action names from questionStore.ts (e.g., `markAsked`, `resetAsked`, `loadAsked` — confirm against the real store).
5. Ensure no test references WatermelonDB `Q`, `database`, or `packDownloader`.

Both files must use the actual exported function/action names from the post-24-02 source — do not invent names. If unsure of a name, grep the source file. Run `npx vitest run apps/mobile/stores/packStore.test.ts apps/mobile/stores/questionStore.test.ts` until green.
  </action>
  <verify>
    <automated>cd apps/mobile && ! grep -rn "watermelondb\|async-storage\|packDownloader\|from '../database'" stores/packStore.test.ts stores/questionStore.test.ts && npx vitest run stores/packStore.test.ts stores/questionStore.test.ts 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "watermelondb\|async-storage\|packDownloader\|from '../database'" apps/mobile/stores/packStore.test.ts apps/mobile/stores/questionStore.test.ts` returns no matches.
    - `npx vitest run apps/mobile/stores/packStore.test.ts apps/mobile/stores/questionStore.test.ts` exits 0.
    - Codebase-wide grep in `apps/mobile` (excluding node_modules, dist/) for `vi.mock('@nozbe/watermelondb'` returns no matches.
    - Codebase-wide grep in `apps/mobile` (excluding node_modules, dist/) for `vi.mock('@react-native-async-storage/async-storage'` returns no matches.
  </acceptance_criteria>
  <done>Both store tests rewritten for the web path; no WatermelonDB/AsyncStorage/packDownloader/database mocks remain in any test file; the two rewritten test files pass.</done>
</task>

<task type="auto">
  <name>Task 2: Update README.md to web/PWA-only, then run the three phase gates</name>
  <files>
    README.md
  </files>
  <read_first>
    README.md (full contents — note lines 38-67 "Running the Apps" with iOS/Android instructions, lines 183-205 "Project Structure" mentioning database/ WatermelonDB, lines 207-226 "Development Commands", lines 228-239 "Tech Stack" mentioning "Database: WatermelonDB"),
    apps/mobile/package.json (post-24-01 — confirm remaining scripts: start, web, build, build:web, test, lint, typecheck)
  </read_first>
  <action>
**README.md updates:**

1. Line 5 ("A mobile trivia game for in-person social play, with a web-based question generator..."): change to "A web/PWA trivia game for in-person social play, with a web-based question generator for creating custom question packs."

2. "Running the Apps" section (lines ~38-67): remove the "### Mobile App (iOS/Android)" subsection and the `pnpm ios` / `pnpm android` / `pnpm web` lines. Replace with a single "### Mobile App (Web/PWA)" subsection:
   ```
   ### Mobile App (Web/PWA)

   ```bash
   # Navigate to mobile app
   cd apps/mobile

   # Start Expo web dev server
   pnpm start
   ```
   The app runs at http://localhost:8081 (Expo web). Production build: `pnpm build:web`.
   ```

3. "Project Structure" section (lines ~183-205): remove the `database/` line (`├── database/     # WatermelonDB models & migrations`) from the tree. Update the `mobile/` comment from "Expo React Native app" to "Expo web/PWA app (web-only — native build path removed in v12.0)".

4. "Tech Stack" section (lines ~228-239): change the "Mobile: Expo SDK 56, React Native 0.85, React 19" line to "Mobile (Web/PWA): Expo SDK 56, React Native 0.85 (web export), React 19". Remove the "Database: WatermelonDB (offline-first)" line and replace with "Storage: IndexedDB (idb-keyval) + sessionStorage (offline-first web)". Keep the rest.

5. "Features" section (line ~16): change "Offline-First: No network required for core gameplay" to "Offline-First PWA: No network required for core gameplay after first load".

Do not change pack-format docs, generator docs, categories, license, or how-to-play sections.

**Phase gates (run after README update and `pnpm install` to prune the lockfile):**

1. `pnpm install --frozen-lockfile=false` (re-run to prune removed deps from node_modules — note: at the repo root, since this is a pnpm monorepo; or `cd apps/mobile && pnpm install` if mobile has its own lockfile. Confirm with `ls pnpm-lock.yaml apps/mobile/pnpm-lock.yaml` — use whichever exists; prefer repo-root install).

2. **Gate 1 — tests:** `cd apps/mobile && npx vitest run` must exit 0 (all tests pass). Per CLAUDE.md: "Never leave known test failures unresolved." If any test fails, fix it in this task (either by updating the test for the new web-only behavior or by deleting it if it covers behavior intentionally removed). Do not skip.

3. **Gate 2 — typecheck:** `cd apps/mobile && npx tsc --noEmit` must exit 0 with NO NEW errors vs. the pre-phase baseline. Per CLAUDE.md: pre-existing errors in unchanged files are acceptable; NEW errors in files this phase touched are NOT acceptable — fix them.

4. **Gate 3 — web build:** `cd apps/mobile && pnpm build:web` must exit 0. This runs `expo export --platform web`, copies public assets, runs generate-statusz and build-sw. If it fails, fix the cause (likely a stale WatermelonDB import somewhere missed in 24-02, or a Tamagui config issue).

If all three gates pass, commit the changes (do not push — the user pushes per their own workflow).
  </action>
  <verify>
    <automated>cd apps/mobile && ! grep -q "WatermelonDB" ../../README.md && ! grep -q "pnpm ios\|pnpm android" ../../README.md && npx vitest run 2>&1 | tail -15 && npx tsc --noEmit 2>&1 | tail -15 && pnpm build:web 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "WatermelonDB" README.md` returns 0.
    - `grep -c "pnpm ios\|pnpm android" README.md` returns 0.
    - `grep -c "database/" README.md` returns 0 (the WatermelonDB database/ line in Project Structure is removed).
    - `cd apps/mobile && npx vitest run` exits 0 (all tests pass).
    - `cd apps/mobile && npx tsc --noEmit` exits 0 OR introduces no NEW errors vs. pre-phase baseline (executor should run and confirm; if new errors appear in files this phase touched, fix them).
    - `cd apps/mobile && pnpm build:web` exits 0 and produces `apps/mobile/dist/`.
    - Commit authored as Faiser <keepbreakfastsimple@gmail.com> (per CLAUDE.md).
  </acceptance_criteria>
  <done>README is web/PWA-only; full vitest suite green; tsc --noEmit clean (no new errors); pnpm build:web succeeds; changes committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test mocks → web services | Test mocks must accurately reflect the post-collapse packCache/questionProvider API or tests give false confidence |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-06 | Repudiation | Tests could pass against wrong mock API and mask a real store bug | mitigate | Task 1 requires the test to use ACTUAL exported function/action names from post-24-02 source (read packCache.ts, questionProvider.ts, packStore.ts, questionStore.ts) rather than invented names. Gate 1 (`npx vitest run`) must be green for the FULL suite, not just the rewritten files. |
| T-24-07 | Denial of Service | Web build could regress due to a missed WatermelonDB import | mitigate | Gate 3 (`pnpm build:web`) requires exit 0; any remaining WatermelonDB reference would fail the Metro bundler. Cross-checked by 24-02 acceptance criteria (no `watermelondb` in production source). |
</threat_model>

<verification>
Phase gates (all three must pass):
1. `cd apps/mobile && npx vitest run` → exit 0.
2. `cd apps/mobile && npx tsc --noEmit` → exit 0 (or no NEW errors vs. baseline).
3. `cd apps/mobile && pnpm build:web` → exit 0, produces dist/.
README is web/PWA-only (no WatermelonDB, no iOS/Android instructions, no database/ in project structure).
No test file mocks @nozbe/watermelondb or @react-native-async-storage/async-storage.
</verification>

<success_criteria>
All three phase gates green. README reflects web/PWA-only deployment. Both rewritten store tests pass. Phase 24 complete — the codebase is web/PWA-only.
</success_criteria>

<output>
After completion, create `.planning/phases/24-remove-native-build-path/24-03-SUMMARY.md`
</output>