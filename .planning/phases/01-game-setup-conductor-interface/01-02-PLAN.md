---
phase: 01-game-setup-conductor-interface
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - app/game/question.tsx
  - components/QuestionCard.tsx
  - components/CategoryBadge.tsx
  - components/AnswerButtons.tsx
  - components/PlayerIndicator.tsx
  - stores/gameStore.ts
  - data/questions/placeholder.ts
autonomous: true
requirements:
  - COND-01
  - COND-02
  - COND-03
  - COND-04
  - COND-05

must_haves:
  truths:
    - "Game conductor sees question text in large, readable font (24pt minimum)"
    - "Game conductor sees category badge with category name and color"
    - "Game conductor sees question number (Q1, Q2, etc.)"
    - "Game conductor can reveal answer by tapping 'Reveal Answer' button"
    - "Game conductor can mark answer as correct or incorrect with large buttons"
    - "Screen shows minimal info during question display (eyes-up design)"
    - "Haptic feedback confirms correct/incorrect marking"
  artifacts:
    - path: "app/game/question.tsx"
      provides: "Question display screen"
      exports: ["default"]
      contains: "QuestionCard"
    - path: "components/QuestionCard.tsx"
      provides: "Question text display with reveal"
      exports: ["QuestionCard"]
      contains: "fontSize.*24"
    - path: "components/CategoryBadge.tsx"
      provides: "Category colored badge"
      exports: ["CategoryBadge"]
    - path: "components/AnswerButtons.tsx"
      provides: "Correct/Incorrect buttons"
      exports: ["AnswerButtons"]
      contains: "Correct"
    - path: "stores/gameStore.ts"
      provides: "Answer reveal state and marking"
      contains: "answerRevealed"
  key_links:
    - from: "app/game/question.tsx"
      to: "useGameStore"
      via: "answerRevealed, revealAnswer, markAnswer"
      pattern: "useGameStore.*answerRevealed"
    - from: "components/AnswerButtons.tsx"
      to: "expo-haptics"
      via: "notificationAsync"
      pattern: "Haptics\\.notificationAsync"
    - from: "components/QuestionCard.tsx"
      to: "components/CategoryBadge"
      via: "import CategoryBadge"
      pattern: "CategoryBadge"
---

<objective>
Create the question display interface with large readable text, category badge, answer reveal, and marking buttons.

Purpose: Enable the game conductor to read questions aloud with minimal screen distractions, reveal answers when ready, and mark answers correct/incorrect with haptic feedback.

Output: Working question screen displaying current question with category badge, reveal button, and marking buttons.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-game-setup-conductor-interface/01-CONTEXT.md
@.planning/phases/01-game-setup-conductor-interface/01-RESEARCH.md
@.planning/research/STACK.md
@.planning/research/ARCHITECTURE.md

<interfaces>
<!-- Key interfaces from Plan 01 that this plan depends on -->

From Plan 01 - stores/gameStore.ts:
```typescript
// Already created in Plan 01
interface GameState {
  phase: GamePhase;
  currentPlayerIndex: number;
  questionNumber: number;
  answerRevealed: boolean;
  startGame: () => void;
  nextTurn: () => void;
  revealAnswer: () => void;
  markAnswer: (correct: boolean) => void;
}

// Will add in this plan:
// - currentQuestion placeholder
// - currentCategory placeholder
```

From Plan 01 - stores/playerStore.ts:
```typescript
// Already created in Plan 01
interface PlayerState {
  players: Player[];
  addPlayer: (name?: string) => void;
  removePlayer: (id: string) => void;
  resetPlayers: () => void;
}
```

From Plan 01 - constants/categories.ts:
```typescript
// Already created in Plan 01
export const PLAYER_COLORS = ['blue', 'pink', 'yellow', 'purple', 'green', 'orange'] as const;

export const CATEGORY_COLORS: Record<PlayerColor, string> = {
  blue: '#0066cc',
  pink: '#ff69b4',
  yellow: '#ffd700',
  purple: '#9932cc',
  green: '#228b22',
  orange: '#ff8c00',
};

// Category names per PROJECT.md:
// Blue: The World Outside
// Pink: Pop Culture & Streaming
// Yellow: Milestones & Myths
// Purple: Animation and Artwork
// Green: Tech, Space & Logic
// Orange: Sports & Gaming
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create placeholder question data</name>
  <files>
    data/questions/placeholder.ts
  </files>
  <read_first>
    data/questions/placeholder.ts
    constants/categories.ts
  </read_first>
  <action>
Create placeholder questions for Phase 1 testing (real question system is Phase 3).

1. Create data/questions/placeholder.ts:
   - Export interface PlaceholderQuestion: category (PlayerColor), questionText (string), answerText (string), difficulty ('easy'|'medium'|'hard')
   - Create PLACEHOLDER_QUESTIONS array with 2-3 questions per category (12-18 total)
   - Questions should be realistic trivia format matching Trivial World categories:
     - Blue (The World Outside): Geography, landmarks, anime settings
     - Pink (Pop Culture & Streaming): Streamers, memes, Marvel, YouTubers
     - Yellow (Milestones & Myths): Tech history, ancient warriors, battles
     - Purple (Animation and Artwork): Comics, graphic novels, video game characters
     - Green (Tech, Space & Logic): AI, astronomy, apex predators
     - Orange (Sports & Gaming): Pro sports, college sports, competitive gaming
   - Export function getRandomQuestion(category?: PlayerColor): PlaceholderQuestion
   - If category provided, filter by category, otherwise random from all
   - Simple Math.random() selection (no repeat tracking in Phase 1)

Per Open Question from RESEARCH.md: Use placeholder questions for Phase 1 development. Real question loading deferred to Phase 3.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    grep "PlaceholderQuestion" data/questions/placeholder.ts
    grep "PLACEHOLDER_QUESTIONS" data/questions/placeholder.ts
    grep "getRandomQuestion" data/questions/placeholder.ts
    grep "blue" data/questions/placeholder.ts
    grep "pink" data/questions/placeholder.ts
    grep "yellow" data/questions/placeholder.ts
    grep "purple" data/questions/placeholder.ts
    grep "green" data/questions/placeholder.ts
    grep "orange" data/questions/placeholder.ts
  </acceptance_criteria>
  <done>
    - data/questions/placeholder.ts exports PLACEHOLDER_QUESTIONS array
    - Each category has 2-3 placeholder questions
    - getRandomQuestion function returns random question (optionally filtered by category)
    - TypeScript compiles without errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Create CategoryBadge component</name>
  <files>
    components/CategoryBadge.tsx
  </files>
  <read_first>
    components/CategoryBadge.tsx
    constants/categories.ts
  </read_first>
  <action>
Create a colored category badge component for question identification.

1. Create components/CategoryBadge.tsx:
   - Props: category (PlayerColor), size (optional, default '$3')
   - Import XStack, Text from tamagui
   - Import CATEGORY_COLORS, PLAYER_COLORS from constants/categories
   - Create mapping of category colors to display names:
     - blue: "The World Outside"
     - pink: "Pop Culture & Streaming"
     - yellow: "Milestones & Myths"
     - purple: "Animation and Artwork"
     - green: "Tech, Space & Logic"
     - orange: "Sports & Gaming"
   - Render: XStack with background color from CATEGORY_COLORS[category], borderRadius, padding
   - Text: category display name, white color, fontWeight bold
   - Per D-10: Category badge displayed prominently with colored background

Use Tamagui theme tokens for sizing. Badge should be visually distinct for reading.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    grep "CategoryBadge" components/CategoryBadge.tsx
    grep "PlayerColor" components/CategoryBadge.tsx
    grep "CATEGORY_COLORS" components/CategoryBadge.tsx
    grep "The World Outside" components/CategoryBadge.tsx
    grep "Pop Culture" components/CategoryBadge.tsx
    grep "XStack" components/CategoryBadge.tsx
    grep "export.*CategoryBadge" components/CategoryBadge.tsx
  </acceptance_criteria>
  <done>
    - CategoryBadge component displays category name on colored background
    - Background color matches category (CATEGORY_COLORS)
    - Category names match Trivial World branding
    - Component accepts size prop for flexibility
    - TypeScript compiles without errors
  </done>
</task>

<task type="auto">
  <name>Task 3: Create QuestionCard component with answer reveal</name>
  <files>
    components/QuestionCard.tsx
  </files>
  <read_first>
    components/QuestionCard.tsx
    components/CategoryBadge.tsx
    constants/categories.ts
  </read_first>
  <action>
Create the main question display component with large text and answer reveal functionality.

1. Create components/QuestionCard.tsx:
   - Props: questionNumber (number), category (PlayerColor), questionText (string), answerText (string), revealed (boolean), onReveal (function)
   - Import YStack, Text, Button from tamagui
   - Import CategoryBadge from ./CategoryBadge
   - Layout: YStack, flex: 1, centered, padding $4, background $background
   - CategoryBadge at top with category prop
   - Question number text: "Q{questionNumber}" in small text, opacity 0.7, margin-top $2 (per D-11)
   - Question text: fontSize 24 MINIMUM (per D-09), fontWeight bold, textAlign center, white color, margin-top $4
   - Answer reveal logic:
     - If not revealed: large "Reveal Answer" button (size $6, margin-top $6, per D-12)
     - If revealed: answer text in fontSize $6, textAlign center, margin-top $4
   - Answer appears below question after reveal (no animation delay per D-12)

Per D-09: Large centered text for arm's-distance reading. Per D-14: Minimal chrome - only category badge, question number, question text, and reveal button visible.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    grep "QuestionCard" components/QuestionCard.tsx
    grep "questionNumber" components/QuestionCard.tsx
    grep "questionText" components/QuestionCard.tsx
    grep "answerText" components/QuestionCard.tsx
    grep "revealed" components/QuestionCard.tsx
    grep "onReveal" components/QuestionCard.tsx
    grep "CategoryBadge" components/QuestionCard.tsx
    grep "Reveal Answer" components/QuestionCard.tsx
    grep "fontSize.*24" components/QuestionCard.tsx
    grep "export.*QuestionCard" components/QuestionCard.tsx
  </acceptance_criteria>
  <done>
    - QuestionCard displays question text at minimum 24pt font
    - CategoryBadge shows prominently at top
    - Question number shows as "Q{N}" format
    - "Reveal Answer" button appears when revealed=false
    - Answer text appears when revealed=true
    - Component uses dark theme colors ($background, $color)
    - TypeScript compiles without errors
  </done>
</task>

<task type="auto">
  <name>Task 4: Create AnswerButtons component with haptic feedback</name>
  <files>
    components/AnswerButtons.tsx
  </files>
  <read_first>
    components/AnswerButtons.tsx
  </read_first>
  <action>
Create the correct/incorrect marking buttons with haptic feedback.

1. Create components/AnswerButtons.tsx:
   - Props: onMark (correct: boolean) => void, visible (boolean, default true)
   - Import XStack, Button from tamagui
   - Import * as Haptics from 'expo-haptics'
   - Layout: XStack, width 100%, gap $2, padding $4
   - Two buttons side by side, each flex: 1 (50% width per D-13)
   - Left button: green background, "Correct" text with checkmark icon, size $6
   - Right button: red background, "Incorrect" text with X icon, size $6
   - Both buttons full-height for easy thumb tapping (per D-13)
   - onPress handler:
     - Call Haptics.notificationAsync with Success for correct, Error for incorrect (per D-20)
     - Call onMark(correct)
   - Only render when visible=true (controlled by answerRevealed state)
   - Use async/await for haptic call (fire-and-forget is acceptable)

Per D-13: 50% width buttons for easy thumb tapping. Per D-20: Haptic feedback on marking.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    grep "AnswerButtons" components/AnswerButtons.tsx
    grep "onMark" components/AnswerButtons.tsx
    grep "visible" components/AnswerButtons.tsx
    grep "Correct" components/AnswerButtons.tsx
    grep "Incorrect" components/AnswerButtons.tsx
    grep "Haptics.notificationAsync" components/AnswerButtons.tsx
    grep "XStack" components/AnswerButtons.tsx
    grep "export.*AnswerButtons" components/AnswerButtons.tsx
  </acceptance_criteria>
  <done>
    - AnswerButtons shows two buttons side by side (50% width each)
    - Correct button is green with checkmark
    - Incorrect button is red with X
    - Buttons trigger haptic feedback (success for correct, error for incorrect)
    - Component only renders when visible=true
    - TypeScript compiles without errors
  </done>
</task>

<task type="auto">
  <name>Task 5: Create PlayerIndicator component</name>
  <files>
    components/PlayerIndicator.tsx
  </files>
  <read_first>
    components/PlayerIndicator.tsx
    constants/categories.ts
  </read_first>
  <action>
Create a minimal player indicator for the question screen header.

1. Create components/PlayerIndicator.tsx:
   - Props: playerName (string), playerColor (PlayerColor)
   - Import XStack, Text from tamagui
   - Import CATEGORY_COLORS from constants/categories
   - Layout: XStack, centered, small padding, background $backgroundHover (subtle)
   - Color dot: small circle with player's category color
   - Player name: text next to dot, color $color
   - Per D-17: Small name/avatar at top of screen showing current player
   - Minimal design - just enough to identify whose turn it is
   - Text should be small (fontSize $2) to not distract from question

This is the only player info visible during question display (per D-14 minimal chrome, D-17 current player indicator).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    grep "PlayerIndicator" components/PlayerIndicator.tsx
    grep "playerName" components/PlayerIndicator.tsx
    grep "playerColor" components/PlayerIndicator.tsx
    grep "CATEGORY_COLORS" components/PlayerIndicator.tsx
    grep "XStack" components/PlayerIndicator.tsx
    grep "export.*PlayerIndicator" components/PlayerIndicator.tsx
  </acceptance_criteria>
  <done>
    - PlayerIndicator shows small color dot and player name
    - Color matches player's assigned color from CATEGORY_COLORS
    - Text is small and non-distracting
    - Component is minimal (eyes-up design principle)
    - TypeScript compiles without errors
  </done>
</task>

<task type="auto">
  <name>Task 6: Update gameStore with question state and placeholder selection</name>
  <files>
    stores/gameStore.ts
  </files>
  <read_first>
    stores/gameStore.ts
    data/questions/placeholder.ts
    constants/categories.ts
  </read_first>
  <action>
Extend the gameStore to manage current question and category.

1. Update stores/gameStore.ts:
   - Import getRandomQuestion from data/questions/placeholder
   - Import PlayerColor from constants/categories
   - Add to GameState interface:
     - currentQuestion: PlaceholderQuestion | null
     - currentCategory: PlayerColor | null
   - Add to initial state:
     - currentQuestion: null
     - currentCategory: null
   - Update startGame() action:
     - Set phase to 'rolling' (Phase 2 will handle die roll, for now jump to answering)
     - For Phase 1 testing: Set currentQuestion to getRandomQuestion()
     - Set currentCategory to currentQuestion.category
   - Add selectCategory(category: PlayerColor) action:
     - Set currentCategory
     - Set currentQuestion to getRandomQuestion(category)
   - Update markAnswer(correct) action:
     - Set answerRevealed to false
     - Increment questionNumber
     - (Phase 2 will handle turn advancement, for now stay on question screen)

Per ARCHITECTURE.md state machine. Phase 1 uses placeholder question selection; real category selection from board position comes in Phase 2-3.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    grep "currentQuestion" stores/gameStore.ts
    grep "currentCategory" stores/gameStore.ts
    grep "getRandomQuestion" stores/gameStore.ts
    grep "selectCategory" stores/gameStore.ts
    grep "PlaceholderQuestion" stores/gameStore.ts
    grep "startGame" stores/gameStore.ts
    grep "markAnswer" stores/gameStore.ts
  </acceptance_criteria>
  <done>
    - gameStore has currentQuestion and currentCategory state
    - startGame() sets a placeholder question for testing
    - selectCategory(category) sets question by category
    - markAnswer() resets answerRevealed and increments questionNumber
    - TypeScript compiles without errors
  </done>
</task>

<task type="auto">
  <name>Task 7: Create question display screen</name>
  <files>
    app/game/question.tsx
  </files>
  <read_first>
    app/game/question.tsx
    stores/gameStore.ts
    stores/playerStore.ts
    components/QuestionCard.tsx
    components/AnswerButtons.tsx
    components/PlayerIndicator.tsx
  </read_first>
  <action>
Create the question display screen that orchestrates all components.

1. Create app/game/question.tsx:
   - Import useRouter from expo-router
   - Import useGameStore, usePlayerStore from stores
   - Import QuestionCard, AnswerButtons, PlayerIndicator from components
   - Import YStack from tamagui
   - Layout: YStack, flex: 1, background $background, padding $4
   - State from gameStore: answerRevealed, revealAnswer, markAnswer, currentQuestion, currentCategory, questionNumber
   - State from playerStore: players, currentPlayerIndex
   - Current player: players[currentPlayerIndex]
   - Render:
     - PlayerIndicator at top with current player name and color
     - QuestionCard in center (flex: 1, centered) with:
       - questionNumber
       - currentCategory (or currentQuestion?.category)
       - currentQuestion?.questionText
       - currentQuestion?.answerText
       - revealed={answerRevealed}
       - onReveal={() => revealAnswer()}
     - AnswerButtons at bottom:
       - visible={answerRevealed}
       - onMark={(correct) => { markAnswer(correct); /* Phase 2 will navigate */ }}
   - Per D-14: No scores, no player list, no board visible - maximum screen space for question
   - Per D-15: Conductor mode is implicit (person holding phone)
   - Per D-16: Auto-advance after marking (Phase 2 will implement turn cycling)

Note: Phase 1 shows static question for testing. Phase 2 will add die roll and category selection.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    grep "QuestionCard" app/game/question.tsx
    grep "AnswerButtons" app/game/question.tsx
    grep "PlayerIndicator" app/game/question.tsx
    grep "useGameStore" app/game/question.tsx
    grep "usePlayerStore" app/game/question.tsx
    grep "answerRevealed" app/game/question.tsx
    grep "revealAnswer" app/game/question.tsx
    grep "markAnswer" app/game/question.tsx
    grep "currentQuestion" app/game/question.tsx
    grep "currentPlayerIndex" app/game/question.tsx
    grep "YStack" app/game/question.tsx
  </acceptance_criteria>
  <done>
    - Question screen displays PlayerIndicator, QuestionCard, and AnswerButtons
    - QuestionCard shows question text at 24pt minimum
    - CategoryBadge displays with correct category color
    - "Reveal Answer" button hides answer until pressed
    - AnswerButtons appear after reveal
    - Marking answer triggers haptic feedback
    - Screen has minimal chrome (eyes-up design)
    - TypeScript compiles without errors
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User input → Store | Question answers are display-only, no security impact |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03 | Information Disclosure | Question content | accept | Questions are non-sensitive trivia content, publicly accessible |
| T-01-04 | Denial of Service | Haptic feedback | accept | Haptic calls are fire-and-forget, cannot block app |
</threat_model>

<verification>
- TypeScript compilation succeeds: `npx tsc --noEmit`
- App launches and navigates from setup to question screen
- Question displays with large text (24pt minimum)
- Category badge shows correct color and name
- "Reveal Answer" button works and shows answer text
- Correct/Incorrect buttons appear after reveal
- Haptic feedback triggers on button press
- Screen maintains minimal chrome design
- Can mark answer and increment question number
</verification>

<success_criteria>
- [ ] Placeholder questions created for all 6 categories
- [ ] CategoryBadge displays category with correct color
- [ ] QuestionCard displays question text at 24pt minimum
- [ ] Question number shows in "Q{N}" format
- [ ] Answer hidden by default, revealed on button press
- [ ] AnswerButtons show 50% width, green/red, with icons
- [ ] Haptic feedback on correct/incorrect marking
- [ ] PlayerIndicator shows current player minimally
- [ ] Question screen has minimal chrome (eyes-up design)
- [ ] All requirements COND-01 through COND-05 implemented
</success_criteria>

<output>
After completion, create `.planning/phases/01-game-setup-conductor-interface/01-02-SUMMARY.md`
</output>