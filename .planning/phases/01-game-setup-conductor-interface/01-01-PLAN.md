---
phase: 01-game-setup-conductor-interface
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/_layout.tsx
  - app/index.tsx
  - app/game/_layout.tsx
  - app/game/setup.tsx
  - stores/gameStore.ts
  - stores/playerStore.ts
  - stores/index.ts
  - constants/categories.ts
  - constants/theme.ts
  - types/game.ts
  - types/player.ts
  - tamagui.config.ts
  - babel.config.js
  - package.json
autonomous: true
requirements:
  - SETUP-01
  - SETUP-02
  - SETUP-03
  - SETUP-04
  - SETUP-05

must_haves:
  truths:
    - "Game conductor can create a new game session from the home screen"
    - "Game conductor can add 1-6 participants with auto-assigned colors"
    - "Game conductor can enter optional participant names (default: Player N)"
    - "Game conductor can remove participants before game starts"
    - "Game conductor can start the game when ready"
  artifacts:
    - path: "app/index.tsx"
      provides: "Home screen with New Game button"
      exports: ["default"]
      contains: "New Game"
    - path: "app/game/setup.tsx"
      provides: "Participant management screen"
      exports: ["default"]
      contains: "Start Game"
    - path: "stores/gameStore.ts"
      provides: "Game phase state machine"
      exports: ["useGameStore"]
      contains: "phase: 'setup'"
    - path: "stores/playerStore.ts"
      provides: "Player CRUD operations"
      exports: ["usePlayerStore"]
      contains: "addPlayer"
    - path: "constants/categories.ts"
      provides: "Player color definitions"
      exports: ["PLAYER_COLORS", "CATEGORY_COLORS"]
  key_links:
    - from: "app/index.tsx"
      to: "/game/setup"
      via: "router.push"
      pattern: "router\\.push.*game/setup"
    - from: "app/game/setup.tsx"
      to: "usePlayerStore"
      via: "addPlayer, removePlayer"
      pattern: "usePlayerStore.*addPlayer"
    - from: "app/game/setup.tsx"
      to: "useGameStore"
      via: "startGame"
      pattern: "startGame\\(\\)"
---

<objective>
Establish the project foundation and game setup flow, enabling game conductors to create new game sessions and manage participants.

Purpose: Build the core infrastructure (Expo Router, Zustand stores, Tamagui theme) and the participant management UI, allowing players to be added, named, removed, and for the game to be started.

Output: Working app with home screen, setup screen, participant management, and Zustand stores persisting game state.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-game-setup-conductor-interface/01-CONTEXT.md
@.planning/phases/01-game-setup-conductor-interface/01-RESEARCH.md
@.planning/research/STACK.md
@.planning/research/ARCHITECTURE.md

<interfaces>
<!-- Key patterns for executor reference - extracted from RESEARCH.md -->

From RESEARCH.md Pattern 1 (Zustand Slice):
```typescript
// stores/gameStore.ts pattern
import { create } from 'zustand';
import { persist, createJSONStorage } from 'zustand/middleware';
import AsyncStorage from '@react-native-async-storage/async-storage';

type GamePhase = 'setup' | 'rolling' | 'moving' | 'answering' | 'scoring' | 'finished';

interface GameState {
  phase: GamePhase;
  currentPlayerIndex: number;
  questionNumber: number;
  answerRevealed: boolean;
  startGame: () => void;
  nextTurn: () => void;
  revealAnswer: () => void;
  markAnswer: (correct: boolean) => void;
}
```

From RESEARCH.md Pattern 3 (Expo Router):
```typescript
// app/_layout.tsx pattern
import { Stack } from 'expo-router';
import { GestureHandlerRootView } from 'react-native-gesture-handler';
import { TamaguiProvider, Theme } from 'tamagui';
import config from '../tamagui.config';

export default function RootLayout() {
  return (
    <GestureHandlerRootView style={{ flex: 1 }}>
      <TamaguiProvider config={config}>
        <Theme name="dark">
          <Stack screenOptions={{ headerShown: false }}>
            <Stack.Screen name="index" />
            <Stack.Screen name="game/setup" />
          </Stack>
        </Theme>
      </TamaguiProvider>
    </GestureHandlerRootView>
  );
}
```

From RESEARCH.md Pattern 4 (Dark Theme):
```typescript
// tamagui.config.ts pattern
import { createTamagui } from 'tamagui';
import { createInterFont } from '@tamagui/font-inter';

const config = createTamagui({
  fonts: {
    heading: createInterFont(),
    body: createInterFont(),
  },
  themes: {
    dark: {
      background: '#1a1a2e',
      color: '#ffffff',
    },
  },
});
```

From RESEARCH.md Code Example (Player Store):
```typescript
// stores/playerStore.ts pattern
import { create } from 'zustand';
import { PLAYER_COLORS, PlayerColor } from '../constants/categories';

interface Player {
  id: string;
  name: string;
  color: PlayerColor;
}

interface PlayerState {
  players: Player[];
  addPlayer: (name?: string) => void;
  removePlayer: (id: string) => void;
  resetPlayers: () => void;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Expo project with dependencies and Tamagui configuration</name>
  <files>
    package.json
    babel.config.js
    tamagui.config.ts
    app.json
  </files>
  <read_first>
    package.json
    app.json
  </read_first>
  <action>
Initialize the Expo SDK 55 project with TypeScript and install all required dependencies.

1. Create babel.config.js with Tamagui compiler plugin before Reanimated plugin (order matters):
   - Presets: babel-preset-expo
   - Plugins: @tamagui/compiler/babel-plugin (first), react-native-reanimated/plugin (last)

2. Create tamagui.config.ts with:
   - Fonts: Inter for heading and body (createInterFont from @tamagui/font-inter)
   - Themes: dark theme with background '#1a1a2e' and color '#ffffff'
   - Category colors matching PLAYER_COLORS (blue: '#0066cc', pink: '#ff69b4', yellow: '#ffd700', purple: '#9932cc', green: '#228b22', orange: '#ff8c00')
   - Export as default

3. Update app.json to include:
   - Bundle identifier: com.trivialworld.app
   - Orientation: portrait (D-xx: lock orientation for consistent conductor experience)
   - plugins: expo-haptics, expo-screen-orientation

4. Install dependencies (verify versions):
   - zustand @react-native-async-storage/async-storage
   - tamagui @tamagui/config @tamagui/font-inter
   - react-native-reanimated react-native-gesture-handler
   - expo-haptics expo-screen-orientation

Per RESEARCH.md standard stack. Do NOT install watermelondb yet (Phase 3 question storage).
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm ls zustand tamagui react-native-reanimated</automated>
  </verify>
  <acceptance_criteria>
    grep "@tamagui/compiler/babel-plugin" babel.config.js
    grep "react-native-reanimated/plugin" babel.config.js
    grep "createTamagui" tamagui.config.ts
    grep "'#1a1a2e'" tamagui.config.ts
    grep "zustand" package.json
  </acceptance_criteria>
  <done>
    - babel.config.js exists with Tamagui before Reanimated plugin order
    - tamagui.config.ts exports createTamagui config with dark theme
    - All dependencies installed without version conflicts
    - TypeScript compiles without errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Create type definitions and category constants</name>
  <files>
    types/game.ts
    types/player.ts
    constants/categories.ts
    constants/theme.ts
  </files>
  <read_first>
    types/game.ts
    types/player.ts
    constants/categories.ts
  </read_first>
  <action>
Create TypeScript type definitions and category constants used throughout the app.

1. Create types/game.ts:
   - GamePhase type: 'setup' | 'rolling' | 'moving' | 'answering' | 'scoring' | 'finished' (per ARCHITECTURE.md state machine)
   - GameState interface with phase, currentPlayerIndex, questionNumber, answerRevealed
   - VALID_TRANSITIONS constant defining valid phase transitions (setup -> rolling -> moving -> answering -> scoring -> rolling|finished)

2. Create types/player.ts:
   - PlayerColor type from PLAYER_COLORS union
   - Player interface: id (string), name (string), color (PlayerColor)
   - PlayerState interface: players array, addPlayer/removePlayer/resetPlayers methods

3. Create constants/categories.ts:
   - PLAYER_COLORS array: ['blue', 'pink', 'yellow', 'purple', 'green', 'orange'] as const (per CONTEXT.md D-05)
   - CATEGORY_COLORS mapping color hex values per RESEARCH.md
   - Export PlayerColor type from this array

4. Create constants/theme.ts:
   - Import Tamagui tokens from config
   - Export color constants for use in components (CATEGORY_COLORS already in categories.ts, so export spacing/font tokens here if needed)

Per ARCHITECTURE.md types folder structure. Types are the foundation for store interfaces.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    grep "GamePhase" types/game.ts
    grep "VALID_TRANSITIONS" types/game.ts
    grep "PlayerColor" types/player.ts
    grep "PLAYER_COLORS" constants/categories.ts
    grep "'blue'" constants/categories.ts
    grep "CATEGORY_COLORS" constants/categories.ts
  </acceptance_criteria>
  <done>
    - types/game.ts exports GamePhase, GameState, VALID_TRANSITIONS
    - types/player.ts exports Player, PlayerState, PlayerColor
    - constants/categories.ts exports PLAYER_COLORS array and CATEGORY_COLORS mapping
    - All types compile without errors
    - VALID_TRANSITIONS has correct phase transition structure
  </done>
</task>

<task type="auto">
  <name>Task 3: Create Zustand stores with persistence</name>
  <files>
    stores/gameStore.ts
    stores/playerStore.ts
    stores/index.ts
  </files>
  <read_first>
    stores/gameStore.ts
    stores/playerStore.ts
    stores/index.ts
    types/game.ts
    types/player.ts
    constants/categories.ts
  </read_first>
  <action>
Create Zustand stores for game state and player management with AsyncStorage persistence.

1. Create stores/gameStore.ts:
   - Import GamePhase, GameState from types/game
   - Use create with persist middleware and createJSONStorage(AsyncStorage)
   - Store name: 'trivial-world-game'
   - Initial state: phase 'setup', currentPlayerIndex 0, questionNumber 1, answerRevealed false
   - Actions: startGame() (sets phase to 'rolling'), revealAnswer(), markAnswer(correct), nextTurn()
   - Export useGameStore hook

2. Create stores/playerStore.ts:
   - Import Player, PlayerState from types/player
   - Import PLAYER_COLORS from constants/categories
   - Use create (no persist needed for player data - will be part of game session)
   - Initial state: empty players array
   - addPlayer(name?):
     - Check length < 6, return unchanged if at max (per D-06)
     - Auto-assign color from PLAYER_COLORS[index]
     - Default name to "Player {index + 1}" if not provided (per D-07)
     - Generate id using crypto.randomUUID()
   - removePlayer(id):
     - Filter out player by id
     - Reassign colors to remaining players (per RESEARCH.md color reassignment)
   - resetPlayers(): clear array
   - Export usePlayerStore hook

3. Create stores/index.ts:
   - Re-export useGameStore, usePlayerStore
   - Export types for convenience

Per RESEARCH.md Pattern 1 (Zustand Slice). Store structure matches ARCHITECTURE.md recommendation.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm test -- --grep="store" 2>/dev/null || echo "No tests yet - will be created in next task"</automated>
  </verify>
  <acceptance_criteria>
    grep "useGameStore" stores/gameStore.ts
    grep "persist" stores/gameStore.ts
    grep "'trivial-world-game'" stores/gameStore.ts
    grep "usePlayerStore" stores/playerStore.ts
    grep "addPlayer" stores/playerStore.ts
    grep "PLAYER_COLORS" stores/playerStore.ts
    grep "export.*useGameStore" stores/index.ts
    grep "export.*usePlayerStore" stores/index.ts
  </acceptance_criteria>
  <done>
    - gameStore persists to AsyncStorage with key 'trivial-world-game'
    - playerStore addPlayer assigns colors in order (blue, pink, yellow, purple, green, orange)
    - playerStore removePlayer reassigns colors correctly
    - Both stores compile and export properly from stores/index.ts
    - TypeScript compilation succeeds
  </done>
</task>

<task type="auto">
  <name>Task 4: Create Expo Router layout with dark theme</name>
  <files>
    app/_layout.tsx
    app/game/_layout.tsx
  </files>
  <read_first>
    app/_layout.tsx
    app/game/_layout.tsx
    tamagui.config.ts
  </read_first>
  <action>
Create Expo Router layout files with Tamagui provider, dark theme, and gesture handler setup.

1. Create app/_layout.tsx (root layout):
   - Wrap with GestureHandlerRootView (required for react-native-gesture-handler)
   - Wrap with TamaguiProvider using the config from tamagui.config.ts
   - Wrap with Theme name="dark" (per D-18 dark theme default)
   - Use Stack navigator with headerShown: false
   - Include screens: index, game (folder for game routes)

2. Create app/game/_layout.tsx (game flow layout):
   - Use Stack navigator
   - Include screens: setup, question
   - No header (headerShown: false)
   - Dark background inheritance from parent

Per RESEARCH.md Pattern 3. Expo Router file-based routing with nested layouts.
  </action>
  <verify>
    <automated>npx expo export --platform ios --output-dir /tmp/expo-test 2>&1 | grep -E "(error|Error)" || echo "Export succeeded"</automated>
  </verify>
  <acceptance_criteria>
    grep "GestureHandlerRootView" app/_layout.tsx
    grep "TamaguiProvider" app/_layout.tsx
    grep 'Theme.*name="dark"' app/_layout.tsx
    grep "Stack" app/_layout.tsx
    grep "headerShown.*false" app/_layout.tsx
    grep "setup" app/game/_layout.tsx
    grep "question" app/game/_layout.tsx
  </acceptance_criteria>
  <done>
    - app/_layout.tsx wraps with GestureHandlerRootView, TamaguiProvider, Theme(dark)
    - app/game/_layout.tsx provides Stack navigator for game screens
    - Both layouts compile and export successfully
    - App starts without runtime errors in Expo development client
  </done>
</task>

<task type="auto">
  <name>Task 5: Create home screen with New Game button</name>
  <files>
    app/index.tsx
  </files>
  <read_first>
    app/index.tsx
    app/_layout.tsx
  </read_first>
  <action>
Create the home screen with a centered "New Game" button that navigates to setup screen.

1. Create app/index.tsx:
   - Use YStack for vertical layout, flex: 1, centered content
   - Background color: $background (from Tamagui dark theme)
   - Title: "Trivial World" in large bold text, color: $color
   - Button: "New Game" using Tamagui Button component
   - size="$5" (large button per accessibility)
   - onPress: router.push('/game/setup')
   - Import useRouter from expo-router

Per D-01 quick start flow. Single button leads directly to participant setup. No setup wizard, no configuration screens.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    grep "Trivial World" app/index.tsx
    grep "New Game" app/index.tsx
    grep "router.push" app/index.tsx
    grep "game/setup" app/index.tsx
    grep "useRouter" app/index.tsx
    grep "YStack" app/index.tsx
  </acceptance_criteria>
  <done>
    - Home screen displays "Trivial World" title and "New Game" button
    - Button onPress navigates to /game/setup route
    - Screen uses dark theme background and centered layout
    - TypeScript compiles without errors
  </done>
</task>

<task type="auto">
  <name>Task 6: Create participant management screen</name>
  <files>
    app/game/setup.tsx
    components/AddPlayerButton.tsx
    components/ParticipantRow.tsx
  </files>
  <read_first>
    app/game/setup.tsx
    components/AddPlayerButton.tsx
    components/ParticipantRow.tsx
    stores/playerStore.ts
    stores/gameStore.ts
    constants/categories.ts
  </read_first>
  <action>
Create the setup screen with inline participant entry, following all locked decisions from CONTEXT.md.

1. Create components/AddPlayerButton.tsx:
   - Props: onPress, disabled (for max 6 players)
   - Tamagui Button with size="$4"
   - Show "+" icon and "Add Participant" text
   - Gray out when disabled (6 players reached)
   - Import from tamagui: Button, Text

2. Create components/ParticipantRow.tsx:
   - Props: player (Player type), onRemove (id: string) => void
   - Use react-native-gesture-handler for swipe-to-remove (per D-08)
   - Import Gesture, GestureDetector from react-native-gesture-handler
   - Import Animated from react-native-reanimated
   - Layout: XStack with player color indicator (colored dot), name text, remove area
   - Swipe left (>100px) triggers onRemove
   - Spring animation back to 0 if swipe incomplete
   - Color indicator: circular dot with background from CATEGORY_COLORS[player.color]
   - Per D-08: swipe-left gesture or long-press for removal

3. Create app/game/setup.tsx:
   - Import usePlayerStore, useGameStore from stores
   - Import ParticipantRow, AddPlayerButton components
   - Import router from expo-router
   - Layout: YStack, scrollable, flex: 1
   - Header: "Setup Game" text at top
   - Participant list: map players from usePlayerStore, render ParticipantRow for each
   - AddPlayerButton below list, disabled when players.length >= 6
   - TextInput for each participant name (inline editing, per D-04)
   - Default value from player.name (Player 1, Player 2, etc. per D-07)
   - onChangeText updates player name in store
   - "Start Game" button at bottom (per D-05)
   - onPress: calls gameStore.startGame(), then router.push('/game/question')
   - Disable Start Game button if players.length === 0 (need at least 1 player per D-06)
   - Use Tamagui Input component for name entry

Per D-04 inline participant entry, D-05 auto-assigned colors, D-06 1-6 players, D-07 optional names, D-08 swipe-to-remove.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    grep "AddPlayerButton" app/game/setup.tsx
    grep "ParticipantRow" app/game/setup.tsx
    grep "usePlayerStore" app/game/setup.tsx
    grep "useGameStore" app/game/setup.tsx
    grep "Start Game" app/game/setup.tsx
    grep "router.push.*game/question" app/game/setup.tsx
    grep "players.length.*===.*0" app/game/setup.tsx
    grep "players.length.*>=.*6" app/game/setup.tsx
    grep "onRemove" components/ParticipantRow.tsx
    grep "Gesture" components/ParticipantRow.tsx
    grep "CATEGORY_COLORS" components/ParticipantRow.tsx
  </acceptance_criteria>
  <done>
    - Setup screen displays participant list with color indicators
    - AddPlayerButton adds players with auto-assigned colors (blue first, pink second, etc.)
    - Participant names are editable inline with TextInput
    - Swipe left on participant removes them and reassigns colors
    - Start Game button navigates to /game/question when players exist
    - Start Game button disabled when no players
    - All components use Tamagui with dark theme
    - TypeScript compiles without errors
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User input → Store | Player names are display-only, no security impact |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | Local storage | accept | Game state is non-sensitive, tampering has no security impact |
| T-01-02 | Information Disclosure | AsyncStorage | accept | No PII stored, only game state (player names are user-provided display strings) |
</threat_model>

<verification>
- TypeScript compilation succeeds: `npx tsc --noEmit`
- App launches in Expo: `npx expo start`
- Home screen displays correctly
- Navigation to setup screen works
- Can add up to 6 players with auto-assigned colors
- Can edit player names inline
- Can remove players via swipe gesture
- Start Game button navigates to question screen
- Game state persists after app reload (check AsyncStorage)
</verification>

<success_criteria>
- [ ] Expo project initialized with SDK 55, TypeScript, Tamagui, Zustand
- [ ] Type definitions created for GamePhase, Player, stores
- [ ] Zustand stores created with AsyncStorage persistence
- [ ] Root layout wraps with GestureHandler, TamaguiProvider, dark Theme
- [ ] Home screen has "New Game" button navigating to /game/setup
- [ ] Setup screen displays participants with color indicators
- [ ] Can add 1-6 players (max limit enforced)
- [ ] Player colors auto-assigned from PLAYER_COLORS array
- [ ] Player names are inline editable with default "Player N"
- [ ] Swipe left removes player and reassigns colors
- [ ] Start Game button navigates to question screen
- [ ] All requirements SETUP-01 through SETUP-05 implemented
</success_criteria>

<output>
After completion, create `.planning/phases/01-game-setup-conductor-interface/01-01-SUMMARY.md`
</output>