---
phase: 09-mobile-web-export
plan: 02
type: execute
wave: 2
depends_on:
  - "09-01"
files_modified:
  - apps/mobile/stores/gameStore.ts
  - apps/mobile/stores/playerStore.ts
  - apps/mobile/stores/questionStore.ts
  - apps/mobile/stores/packStore.ts
autonomous: true
requirements:
  - WEBG-02
must_haves:
  truths:
    - "Game state persists during browser session (refresh keeps state)"
    - "Player data persists during browser session"
    - "Asked questions tracking persists during browser session"
    - "Pack selection persists during browser session"
  artifacts:
    - path: "apps/mobile/stores/gameStore.ts"
      provides: "Game state with platform storage"
      contains: "platformStorage"
    - path: "apps/mobile/stores/playerStore.ts"
      provides: "Player state with platform storage"
      contains: "platformStorage"
    - path: "apps/mobile/stores/questionStore.ts"
      provides: "Question tracking with platform storage"
      contains: "platformStorage"
    - path: "apps/mobile/stores/packStore.ts"
      provides: "Pack state with platform storage"
      contains: "platformStorage"
  key_links:
    - from: "stores/*.ts"
      to: "services/platformStorage"
      via: "import"
      pattern: "from '../services/platformStorage'"
---

<objective>
Update all Zustand stores to use the platform storage adapter instead of AsyncStorage.

Purpose: Enable session-only storage on web while maintaining AsyncStorage persistence on mobile.
Output: All stores using platformStorage for persistence.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-mobile-web-export/09-CONTEXT.md
@.planning/phases/09-mobile-web-export/09-01-PLAN.md

<interfaces>
<!-- Storage adapter from Wave 1 -->

From apps/mobile/services/platformStorage.ts (created in Wave 1):
```typescript
export const platformStorage = createPlatformStorage();
```

From apps/mobile/stores/gameStore.ts (current persist pattern, lines 247-255):
```typescript
import AsyncStorage from '@react-native-async-storage/async-storage';
// ...
persist(
  (set, get) => ({ /* store */ }),
  {
    name: 'trivial-world-game',
    storage: createJSONStorage(() => AsyncStorage),  // <-- CHANGE to platformStorage
    partialize: (state) => ({
      activePackId: state.activePackId,
    }),
  }
)
```

From apps/mobile/stores/playerStore.ts (current persist pattern, lines 1-4):
```typescript
import { create } from 'zustand';
import { persist, createJSONStorage } from 'zustand/middleware';
import AsyncStorage from '@react-native-async-storage/async-storage';
// Store uses AsyncStorage in persist middleware
```

From apps/mobile/stores/questionStore.ts (current persist pattern):
```typescript
// Uses AsyncStorage with custom Set serialization
// Similar pattern to gameStore
```

From apps/mobile/stores/packStore.ts (current persist pattern):
```typescript
// Uses AsyncStorage for pack state persistence
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update gameStore to use platformStorage</name>
  <files>apps/mobile/stores/gameStore.ts</files>
  <read_first>
    - apps/mobile/stores/gameStore.ts (current implementation)
    - apps/mobile/services/platformStorage.ts (created in Wave 1)
    - .planning/phases/09-mobile-web-export/09-CONTEXT.md (D-05 decision)
  </read_first>
  <action>
    Replace AsyncStorage import and usage with platformStorage in gameStore.ts per D-05:

    Changes required:
    1. Remove AsyncStorage import:
       - DELETE: `import AsyncStorage from '@react-native-async-storage/async-storage';`

    2. Add platformStorage import:
       - ADD: `import { platformStorage } from '../services/platformStorage';`

    3. Update persist middleware configuration (around line 247-255):
       - CHANGE: `storage: createJSONStorage(() => AsyncStorage)`
       - TO: `storage: createJSONStorage(() => platformStorage)`

    The persist middleware configuration should now read:
    ```typescript
    {
      name: 'trivial-world-game',
      storage: createJSONStorage(() => platformStorage),
      partialize: (state) => ({
        activePackId: state.activePackId,
      }),
    }
    ```

    No other changes needed. The store logic remains identical.
  </action>
  <acceptance_criteria>
    - apps/mobile/stores/gameStore.ts does NOT contain 'AsyncStorage' import
    - apps/mobile/stores/gameStore.ts contains "from '../services/platformStorage'"
    - apps/mobile/stores/gameStore.ts contains "createJSONStorage(() => platformStorage)"
    - Store name remains 'trivial-world-game'
  </acceptance_criteria>
  <verify>
    <automated>grep -n 'AsyncStorage' apps/mobile/stores/gameStore.ts && echo "FAIL: AsyncStorage still present" || echo "PASS: No AsyncStorage"; grep -n "platformStorage" apps/mobile/stores/gameStore.ts</automated>
  </verify>
  <done>gameStore uses platformStorage for persistence</done>
</task>

<task type="auto">
  <name>Task 2: Update playerStore to use platformStorage</name>
  <files>apps/mobile/stores/playerStore.ts</files>
  <read_first>
    - apps/mobile/stores/playerStore.ts (current implementation)
    - apps/mobile/services/platformStorage.ts (created in Wave 1)
    - .planning/phases/09-mobile-web-export/09-CONTEXT.md (D-05 decision)
  </read_first>
  <action>
    Replace AsyncStorage import and usage with platformStorage in playerStore.ts per D-05:

    Changes required:
    1. Remove AsyncStorage import:
       - DELETE: `import AsyncStorage from '@react-native-async-storage/async-storage';`

    2. Add platformStorage import:
       - ADD: `import { platformStorage } from '../services/platformStorage';`

    3. Update persist middleware storage configuration:
       - CHANGE: `storage: createJSONStorage(() => AsyncStorage)`
       - TO: `storage: createJSONStorage(() => platformStorage)`

    The persist middleware configuration should now use platformStorage instead of AsyncStorage.
    Store name should remain unchanged (likely 'trivial-world-players' or similar).
  </action>
  <acceptance_criteria>
    - apps/mobile/stores/playerStore.ts does NOT contain 'AsyncStorage' import
    - apps/mobile/stores/playerStore.ts contains "from '../services/platformStorage'"
    - apps/mobile/stores/playerStore.ts contains "createJSONStorage(() => platformStorage)"
  </acceptance_criteria>
  <verify>
    <automated>grep -n 'AsyncStorage' apps/mobile/stores/playerStore.ts && echo "FAIL: AsyncStorage still present" || echo "PASS: No AsyncStorage"; grep -n "platformStorage" apps/mobile/stores/playerStore.ts</automated>
  </verify>
  <done>playerStore uses platformStorage for persistence</done>
</task>

<task type="auto">
  <name>Task 3: Update questionStore to use platformStorage</name>
  <files>apps/mobile/stores/questionStore.ts</files>
  <read_first>
    - apps/mobile/stores/questionStore.ts (current implementation with Set serialization)
    - apps/mobile/services/platformStorage.ts (created in Wave 1)
    - .planning/phases/09-mobile-web-export/09-CONTEXT.md (D-05 decision)
  </read_first>
  <action>
    Replace AsyncStorage import and usage with platformStorage in questionStore.ts per D-05:

    This store has custom Set serialization for askedQuestions. Preserve that logic.

    Changes required:
    1. Remove AsyncStorage import:
       - DELETE: `import AsyncStorage from '@react-native-async-storage/async-storage';`

    2. Add platformStorage import:
       - ADD: `import { platformStorage } from '../services/platformStorage';`

    3. Update persist middleware storage configuration:
       - CHANGE: `storage: createJSONStorage(() => AsyncStorage)`
       - TO: `storage: createJSONStorage(() => platformStorage)`

    Preserve any partialize or onRehydrateStorage logic that handles Set serialization.
    The custom serialization logic remains identical, only the storage backend changes.
  </action>
  <acceptance_criteria>
    - apps/mobile/stores/questionStore.ts does NOT contain 'AsyncStorage' import
    - apps/mobile/stores/questionStore.ts contains "from '../services/platformStorage'"
    - apps/mobile/stores/questionStore.ts contains "createJSONStorage(() => platformStorage)"
    - Set serialization logic preserved (partialize/onRehydrateStorage if present)
  </acceptance_criteria>
  <verify>
    <automated>grep -n 'AsyncStorage' apps/mobile/stores/questionStore.ts && echo "FAIL: AsyncStorage still present" || echo "PASS: No AsyncStorage"; grep -n "platformStorage" apps/mobile/stores/questionStore.ts</automated>
  </verify>
  <done>questionStore uses platformStorage for persistence</done>
</task>

<task type="auto">
  <name>Task 4: Update packStore to use platformStorage</name>
  <files>apps/mobile/stores/packStore.ts</files>
  <read_first>
    - apps/mobile/stores/packStore.ts (current implementation)
    - apps/mobile/services/platformStorage.ts (created in Wave 1)
    - .planning/phases/09-mobile-web-export/09-CONTEXT.md (D-05 decision)
  </read_first>
  <action>
    Replace AsyncStorage import and usage with platformStorage in packStore.ts per D-05:

    Changes required:
    1. Remove AsyncStorage import:
       - DELETE: `import AsyncStorage from '@react-native-async-storage/async-storage';`

    2. Add platformStorage import:
       - ADD: `import { platformStorage } from '../services/platformStorage';`

    3. Update persist middleware storage configuration:
       - CHANGE: `storage: createJSONStorage(() => AsyncStorage)`
       - TO: `storage: createJSONStorage(() => platformStorage)`

    The store name and any other configuration remain unchanged.
  </action>
  <acceptance_criteria>
    - apps/mobile/stores/packStore.ts does NOT contain 'AsyncStorage' import
    - apps/mobile/stores/packStore.ts contains "from '../services/platformStorage'"
    - apps/mobile/stores/packStore.ts contains "createJSONStorage(() => platformStorage)"
  </acceptance_criteria>
  <verify>
    <automated>grep -n 'AsyncStorage' apps/mobile/stores/packStore.ts && echo "FAIL: AsyncStorage still present" || echo "PASS: No AsyncStorage"; grep -n "platformStorage" apps/mobile/stores/packStore.ts</automated>
  </verify>
  <done>packStore uses platformStorage for persistence</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Zustand stores → platformStorage | Platform-aware persistence layer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-04 | Information Disclosure | sessionStorage | accept | Per D-06: session-only storage intentional; no PII in game state |

No changes to threat model from Wave 1. Storage adapter isolates platform differences.
</threat_model>

<verification>
- TypeScript compiles: `cd apps/mobile && pnpm typecheck`
- No AsyncStorage imports remain in stores: `grep -r "from '@react-native-async-storage/async-storage'" apps/mobile/stores/ && echo "FAIL" || echo "PASS"`
- All stores import platformStorage: `grep -l "platformStorage" apps/mobile/stores/*.ts`
</verification>

<success_criteria>
- All four stores (gameStore, playerStore, questionStore, packStore) use platformStorage
- No AsyncStorage imports in any store file
- TypeScript compilation succeeds
- Store names and configuration unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/09-mobile-web-export/09-02-SUMMARY.md`
</output>