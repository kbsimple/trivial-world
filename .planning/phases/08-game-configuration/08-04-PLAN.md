---
phase: 08-game-configuration
plan: 04
type: execute
wave: 4
depends_on:
  - 08-01
  - 08-02
  - 08-03
gap_closure: true
files_modified:
  - apps/mobile/database/index.ts
  - apps/mobile/services/packDownloader.ts
  - apps/mobile/stores/questionStore.ts
  - apps/mobile/stores/gameStore.ts
  - apps/mobile/database/migrations/003_seed_default_pack.ts
  - apps/mobile/app/packs/index.tsx
autonomous: true
requirements:
  - CLOUD-02 (fix)
  - CONF-01 (fix)
---

<objective>
Fix blocking gaps found in verification: database export, TypeScript type errors, and async/sync mismatches.

Purpose: Resolve all blocker issues preventing the phase from passing verification.
Output: Fixed TypeScript errors, proper database export, corrected async handling.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-game-configuration/08-VERIFICATION.md
</context>

<tasks>
<task id="1">
<name>Export database instance from database/index.ts</name>
<files>
- apps/mobile/database/index.ts
</files>
<action>
Fix the database export issue. The file apps/mobile/database/index.ts currently only exports the Database type and database creation function, but not the actual database instance. Other files are importing `database` from this path but it doesn't exist.

Read apps/mobile/database/index.ts to see the current exports.
Read apps/mobile/app/_layout.tsx to see how the database is created and initialized.
Add a `database` export that other files can import. The database should be created using the databaseFactory pattern established in Phase 6.

The fix should:
1. Export the database instance created by createDatabase()
2. Ensure the database is initialized before any queries run
3. Maintain the existing initialization pattern from _layout.tsx

Acceptance criteria:
- `export const database` exists in apps/mobile/database/index.ts
- Files importing `database` from `../database` resolve correctly
- No TypeScript errors for database imports
</action>
<read_first>
- apps/mobile/database/index.ts
- apps/mobile/app/_layout.tsx
- apps/mobile/database/models/QuestionPack.ts
</read_first>
<verify>
<automated>
grep -n "export.*database" apps/mobile/database/index.ts
npx tsc --noEmit 2>&1 | grep -c "database" | head -1
</automated>
</verify>
<done>
Database instance is now exported and all imports resolve correctly.
</done>
</task>

<task id="2">
<name>Fix TypeScript type errors in packDownloader.ts</name>
<files>
- apps/mobile/services/packDownloader.ts
</files>
<action>
Fix the TypeScript errors on lines 131-133 where `null` is assigned to optional fields. In TypeScript, optional fields (defined with `?`) expect `undefined` not `null` when not set.

Read apps/mobile/services/packDownloader.ts and find the lines where null is assigned to optional fields.
Change `null` to `undefined` for:
- askedAt field
- downloadedAt field  
- Any other optional fields assigned null

The fix ensures type safety for the Question and QuestionPack model fields.
</action>
<read_first>
- apps/mobile/services/packDownloader.ts
- packages/types/src/question-pack.ts
</read_first>
<verify>
<automated>
npx tsc --noEmit 2>&1 | grep -c "packDownloader" | head -1
</automated>
</verify>
<done>
TypeScript errors in packDownloader.ts resolved - no null assignments to optional types.
</done>
</task>

<task id="3">
<name>Fix TypeScript type errors in questionStore.ts and migration</name>
<files>
- apps/mobile/stores/questionStore.ts
- apps/mobile/database/migrations/003_seed_default_pack.ts
</files>
<action>
Fix TypeScript errors in questionStore.ts and the seed migration file.

Read apps/mobile/stores/questionStore.ts to find:
- Lines 61, 144, 166: Import errors for database
- Lines 119, 156, 188: QuestionModel type usage errors

Read apps/mobile/database/migrations/003_seed_default_pack.ts to find:
- Line 1: Import error for database
- Line 79: askedAt null type error

Fixes:
1. Ensure database is imported from the correct path (after Task 1 fix)
2. Use correct QuestionModel types for queries
3. Change null to undefined for askedAt field in the migration

The QuestionModel should be imported from the correct path and used consistently.
</action>
<read_first>
- apps/mobile/stores/questionStore.ts
- apps/mobile/database/migrations/003_seed_default_pack.ts
- apps/mobile/database/models/Question.ts
</read_first>
<verify>
<automated>
npx tsc --noEmit 2>&1 | grep -c "questionStore" | head -1
npx tsc --noEmit 2>&1 | grep -c "seed_default_pack" | head -1
</automated>
</verify>
<done>
TypeScript errors in questionStore.ts and migration file resolved.
</done>
</task>

<task id="4">
<name>Fix async/sync mismatch in gameStore.ts</name>
<files>
- apps/mobile/stores/gameStore.ts
</files>
<action>
Fix the async/sync mismatch where `selectQuestion` returns a Promise but is called synchronously.

Read apps/mobile/stores/gameStore.ts to find lines 69, 108, 181, 193 where selectQuestion is called without await.

Read apps/mobile/stores/questionStore.ts to confirm selectQuestion is async and returns Promise<Question | null>.

The fix requires:
1. Making the calling functions in gameStore.ts async (mark them as async functions)
2. Adding `await` to selectQuestion calls
3. Handling the async nature properly in Zustand store actions

This may require restructuring some store actions to be async, or using the sync pattern with proper Promise handling.

Acceptance criteria:
- No TypeScript errors about Promise vs Question type mismatch
- selectQuestion calls use await or proper Promise handling
- Game flow still works correctly with async question selection
</action>
<read_first>
- apps/mobile/stores/gameStore.ts
- apps/mobile/stores/questionStore.ts
</read_first>
<verify>
<automated>
npx tsc --noEmit 2>&1 | grep -c "gameStore" | head -1
</automated>
</verify>
<done>
Async/sync mismatch resolved - selectQuestion properly awaited or handled.
</done>
</task>

<task id="5">
<name>Wire bytesWritten to DownloadProgress component</name>
<files>
- apps/mobile/app/packs/index.tsx
</files>
<action>
Fix the placeholder `bytesWritten={0}` in the DownloadProgress component to show actual bytes transferred during download.

Read apps/mobile/app/packs/index.tsx to find the DownloadProgress component usage.
Read apps/mobile/services/packDownloader.ts to understand the progress callback structure.

The progress callback provides `bytesWritten` and `totalBytes`. Track this in state and pass it to the DownloadProgress component.

The fix should:
1. Add a state variable to track bytesWritten
2. Update it in the download progress callback
3. Pass it to DownloadProgress instead of hardcoded 0

This is a warning-level issue but improves user experience by showing accurate download progress.
</action>
<read_first>
- apps/mobile/app/packs/index.tsx
- apps/mobile/components/DownloadProgress.tsx
- apps/mobile/services/packDownloader.ts
</read_first>
<verify>
<automated>
grep -n "bytesWritten" apps/mobile/app/packs/index.tsx | grep -v "= 0"
</automated>
</verify>
<done>
DownloadProgress now shows actual bytes transferred, not hardcoded 0.
</done>
</task>

<task id="6">
<name>Run TypeScript check and fix remaining errors</name>
<files>
- apps/mobile/stores/questionStore.ts
- apps/mobile/stores/gameStore.ts
- apps/mobile/services/packDownloader.ts
- apps/mobile/database/index.ts
</files>
<action>
Run full TypeScript check to verify all errors are resolved.

Execute: npx tsc --noEmit

If any remaining errors exist in Phase 8 files:
1. Fix type mismatches
2. Fix import paths
3. Fix null vs undefined issues
4. Fix any other TypeScript errors

The goal is clean TypeScript compilation with no errors in Phase 8 files.
</action>
<read_first>
- apps/mobile/stores/questionStore.ts
- apps/mobile/stores/gameStore.ts
- apps/mobile/services/packDownloader.ts
- apps/mobile/database/index.ts
</read_first>
<verify>
<automated>
npx tsc --noEmit 2>&1 | tail -20
</automated>
</verify>
<done>
TypeScript compilation passes with no errors in Phase 8 files.
</done>
</task>
</tasks>

<must_haves>
  truths:
    - "All TypeScript errors in Phase 8 files are resolved"
    - "Database instance is properly exported and importable"
    - "Async functions are called with await"
    - "Download progress shows actual bytes, not placeholder"
  artifacts:
    - path: "apps/mobile/database/index.ts"
      provides: "Exported database instance for all consumers"
      exports: ["database"]
    - path: "apps/mobile/services/packDownloader.ts"
      provides: "Type-safe download service"
    - path: "apps/mobile/stores/gameStore.ts"
      provides: "Properly async game store"
    - path: "apps/mobile/stores/questionStore.ts"
      provides: "Type-safe question queries"
    - path: "apps/mobile/app/packs/index.tsx"
      provides: "Accurate download progress display"
</must_haves>

<verification>
All must_haves verified via TypeScript compilation and code review.
No blockers remain for Phase 8 completion.
</verification>

<threat_model>
No new security concerns - this is a fix task for existing code.
</threat_model>